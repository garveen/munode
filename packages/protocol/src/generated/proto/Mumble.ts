// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               v3.21.12
// source: Mumble.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "mumbleproto";

export interface Version {
  /** 2-byte Major, 1-byte Minor and 1-byte Patch version number. */
  version?:
    | number
    | undefined;
  /** Client release name. */
  release?:
    | string
    | undefined;
  /** Client OS name. */
  os?:
    | string
    | undefined;
  /** Client OS version. */
  os_version?: string | undefined;
}

/** Not used. Not even for tunneling UDP through TCP. */
export interface UDPTunnel {
  /** Not used. */
  packet?: Buffer | undefined;
}

/** Used by the client to send the authentication credentials to the server. */
export interface Authenticate {
  /** UTF-8 encoded username. */
  username?:
    | string
    | undefined;
  /** Server or user password. */
  password?:
    | string
    | undefined;
  /** Additional access tokens for server ACL groups. */
  tokens?:
    | string[]
    | undefined;
  /** A list of CELT bitstream version constants supported by the client. */
  celt_versions?: number[] | undefined;
  opus?: boolean | undefined;
}

/**
 * Sent by the client to notify the server that the client is still alive.
 * Server must reply to the packet with the same timestamp and its own
 * good/late/lost/resync numbers. None of the fields is strictly required.
 */
export interface Ping {
  /** Client timestamp. Server should not attempt to decode. */
  timestamp?:
    | number
    | undefined;
  /** The amount of good packets received. */
  good?:
    | number
    | undefined;
  /** The amount of late packets received. */
  late?:
    | number
    | undefined;
  /** The amount of packets never received. */
  lost?:
    | number
    | undefined;
  /** The amount of nonce resyncs. */
  resync?:
    | number
    | undefined;
  /** The total amount of UDP packets received. */
  udp_packets?:
    | number
    | undefined;
  /** The total amount of TCP packets received. */
  tcp_packets?:
    | number
    | undefined;
  /** UDP ping average. */
  udp_ping_avg?:
    | number
    | undefined;
  /** UDP ping variance. */
  udp_ping_var?:
    | number
    | undefined;
  /** TCP ping average. */
  tcp_ping_avg?:
    | number
    | undefined;
  /** TCP ping variance. */
  tcp_ping_var?: number | undefined;
}

/** Sent by the server when it rejects the user connection. */
export interface Reject {
  /** Rejection type. */
  type?:
    | Reject_RejectType
    | undefined;
  /** Human readable rejection reason. */
  reason?: string | undefined;
}

export enum Reject_RejectType {
  /**
   * None - The rejection reason is unknown (details should be available
   * in Reject.reason).
   */
  None = 0,
  /** WrongVersion - The client attempted to connect with an incompatible version. */
  WrongVersion = 1,
  /** InvalidUsername - The user name supplied by the client was invalid. */
  InvalidUsername = 2,
  /**
   * WrongUserPW - The client attempted to authenticate as a user with a password but it
   * was wrong.
   */
  WrongUserPW = 3,
  /**
   * WrongServerPW - The client attempted to connect to a passworded server but the password
   * was wrong.
   */
  WrongServerPW = 4,
  /** UsernameInUse - Supplied username is already in use. */
  UsernameInUse = 5,
  /** ServerFull - Server is currently full and cannot accept more users. */
  ServerFull = 6,
  /** NoCertificate - The user did not provide a certificate but one is required. */
  NoCertificate = 7,
  AuthenticatorFail = 8,
  UNRECOGNIZED = -1,
}

export function reject_RejectTypeFromJSON(object: any): Reject_RejectType {
  switch (object) {
    case 0:
    case "None":
      return Reject_RejectType.None;
    case 1:
    case "WrongVersion":
      return Reject_RejectType.WrongVersion;
    case 2:
    case "InvalidUsername":
      return Reject_RejectType.InvalidUsername;
    case 3:
    case "WrongUserPW":
      return Reject_RejectType.WrongUserPW;
    case 4:
    case "WrongServerPW":
      return Reject_RejectType.WrongServerPW;
    case 5:
    case "UsernameInUse":
      return Reject_RejectType.UsernameInUse;
    case 6:
    case "ServerFull":
      return Reject_RejectType.ServerFull;
    case 7:
    case "NoCertificate":
      return Reject_RejectType.NoCertificate;
    case 8:
    case "AuthenticatorFail":
      return Reject_RejectType.AuthenticatorFail;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Reject_RejectType.UNRECOGNIZED;
  }
}

export function reject_RejectTypeToJSON(object: Reject_RejectType): string {
  switch (object) {
    case Reject_RejectType.None:
      return "None";
    case Reject_RejectType.WrongVersion:
      return "WrongVersion";
    case Reject_RejectType.InvalidUsername:
      return "InvalidUsername";
    case Reject_RejectType.WrongUserPW:
      return "WrongUserPW";
    case Reject_RejectType.WrongServerPW:
      return "WrongServerPW";
    case Reject_RejectType.UsernameInUse:
      return "UsernameInUse";
    case Reject_RejectType.ServerFull:
      return "ServerFull";
    case Reject_RejectType.NoCertificate:
      return "NoCertificate";
    case Reject_RejectType.AuthenticatorFail:
      return "AuthenticatorFail";
    case Reject_RejectType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ServerSync message is sent by the server when it has authenticated the user
 * and finished synchronizing the server state.
 */
export interface ServerSync {
  /** The session of the current user. */
  session?:
    | number
    | undefined;
  /** Maximum bandwidth that the user should use. */
  max_bandwidth?:
    | number
    | undefined;
  /** Server welcome text. */
  welcome_text?:
    | string
    | undefined;
  /**
   * Current user permissions in the root channel.
   * Note: The permissions data type usually is uin32 (e.g. in PermissionQuery and PermissionDenied messages). Here
   * it is uint64 because of an oversight in the past. Nonetheless it should never exceed the uin32 range.
   * See also: https://github.com/mumble-voip/mumble/issues/5139
   */
  permissions?: number | undefined;
}

/**
 * Sent by the client when it wants a channel removed. Sent by the server when
 * a channel has been removed and clients should be notified.
 */
export interface ChannelRemove {
  channel_id?: number | undefined;
}

/**
 * Used to communicate channel properties between the client and the server.
 * Sent by the server during the login process or when channel properties are
 * updated. Client may use this message to update said channel properties.
 */
export interface ChannelState {
  /** Unique ID for the channel within the server. */
  channel_id?:
    | number
    | undefined;
  /** channel_id of the parent channel. */
  parent?:
    | number
    | undefined;
  /** UTF-8 encoded channel name. */
  name?:
    | string
    | undefined;
  /**
   * A collection of channel id values of the linked channels. Absent during
   * the first channel listing.
   */
  links?:
    | number[]
    | undefined;
  /**
   * UTF-8 encoded channel description. Only if the description is less than
   * 128 bytes
   */
  description?:
    | string
    | undefined;
  /** A collection of channel_id values that should be added to links. */
  links_add?:
    | number[]
    | undefined;
  /** A collection of channel_id values that should be removed from links. */
  links_remove?:
    | number[]
    | undefined;
  /** True if the channel is temporary. */
  temporary?:
    | boolean
    | undefined;
  /** Position weight to tweak the channel position in the channel list. */
  position?:
    | number
    | undefined;
  /** SHA1 hash of the description if the description is 128 bytes or more. */
  description_hash?:
    | Buffer
    | undefined;
  /**
   * Maximum number of users allowed in the channel. If this value is zero,
   * the maximum number of users allowed in the channel is given by the
   * server's "usersperchannel" setting.
   */
  max_users?:
    | number
    | undefined;
  /** Whether this channel has enter restrictions (ACL denying ENTER) set */
  is_enter_restricted?:
    | boolean
    | undefined;
  /** Whether the receiver of this msg is considered to be able to enter this channel */
  can_enter?: boolean | undefined;
}

/**
 * Used to communicate user leaving or being kicked. May be sent by the client
 * when it attempts to kick a user. Sent by the server when it informs the
 * clients that a user is not present anymore.
 */
export interface UserRemove {
  /**
   * The user who is being kicked, identified by their session, not present
   * when no one is being kicked.
   */
  session?:
    | number
    | undefined;
  /**
   * The user who initiated the removal. Either the user who performs the kick
   * or the user who is currently leaving.
   */
  actor?:
    | number
    | undefined;
  /** Reason for the kick, stored as the ban reason if the user is banned. */
  reason?:
    | string
    | undefined;
  /** True if the kick should result in a ban. */
  ban?: boolean | undefined;
}

/**
 * Sent by the server when it communicates new and changed users to client.
 * First seen during login procedure. May be sent by the client when it wishes
 * to alter its state.
 */
export interface UserState {
  /**
   * Unique user session ID of the user whose state this is, may change on
   * reconnect.
   */
  session?:
    | number
    | undefined;
  /** The session of the user who is updating this user. */
  actor?:
    | number
    | undefined;
  /** User name, UTF-8 encoded. */
  name?:
    | string
    | undefined;
  /** Registered user ID if the user is registered. */
  user_id?:
    | number
    | undefined;
  /** Channel on which the user is. */
  channel_id?:
    | number
    | undefined;
  /** True if the user is muted by admin. */
  mute?:
    | boolean
    | undefined;
  /** True if the user is deafened by admin. */
  deaf?:
    | boolean
    | undefined;
  /**
   * True if the user has been suppressed from talking by a reason other than
   * being muted.
   */
  suppress?:
    | boolean
    | undefined;
  /** True if the user has muted self. */
  self_mute?:
    | boolean
    | undefined;
  /** True if the user has deafened self. */
  self_deaf?:
    | boolean
    | undefined;
  /** User image if it is less than 128 bytes. */
  texture?:
    | Buffer
    | undefined;
  /**
   * The positional audio plugin identifier.
   * Positional audio information is only sent to users who share
   * identical plugin contexts.
   *
   * This value is not transmitted to clients.
   */
  plugin_context?:
    | Buffer
    | undefined;
  /**
   * The user's plugin-specific identity.
   * This value is not transmitted to clients.
   */
  plugin_identity?:
    | string
    | undefined;
  /** User comment if it is less than 128 bytes. */
  comment?:
    | string
    | undefined;
  /** The hash of the user certificate. */
  hash?:
    | string
    | undefined;
  /** SHA1 hash of the user comment if it 128 bytes or more. */
  comment_hash?:
    | Buffer
    | undefined;
  /** SHA1 hash of the user picture if it 128 bytes or more. */
  texture_hash?:
    | Buffer
    | undefined;
  /** True if the user is a priority speaker. */
  priority_speaker?:
    | boolean
    | undefined;
  /** True if the user is currently recording. */
  recording?:
    | boolean
    | undefined;
  /** A list of temporary access tokens to be respected when processing this request. */
  temporary_access_tokens?:
    | string[]
    | undefined;
  /** A list of channels the user wants to start listening to. */
  listening_channel_add?:
    | number[]
    | undefined;
  /** a list of channels the user does no longer want to listen to. */
  listening_channel_remove?: number[] | undefined;
}

/**
 * Relays information on the bans. The client may send the BanList message to
 * either modify the list of bans or query them from the server. The server
 * sends this list only after a client queries for it.
 */
export interface BanList {
  /** List of ban entries currently in place. */
  bans?:
    | BanList_BanEntry[]
    | undefined;
  /**
   * True if the server should return the list, false if it should replace old
   * ban list with the one provided.
   */
  query?: boolean | undefined;
}

export interface BanList_BanEntry {
  /** Banned IP address. */
  address?:
    | Buffer
    | undefined;
  /** The length of the subnet mask for the ban. */
  mask?:
    | number
    | undefined;
  /** User name for identification purposes (does not affect the ban). */
  name?:
    | string
    | undefined;
  /** The certificate hash of the banned user. */
  hash?:
    | string
    | undefined;
  /** Reason for the ban (does not affect the ban). */
  reason?:
    | string
    | undefined;
  /** Ban start time. */
  start?:
    | string
    | undefined;
  /** Ban duration in seconds. */
  duration?: number | undefined;
}

/** Used to send and broadcast text messages. */
export interface TextMessage {
  /** The message sender, identified by its session. */
  actor?:
    | number
    | undefined;
  /** Target users for the message, identified by their session. */
  session?:
    | number[]
    | undefined;
  /**
   * The channels to which the message is sent, identified by their
   * channel_ids.
   */
  channel_id?:
    | number[]
    | undefined;
  /**
   * The root channels when sending message recursively to several channels,
   * identified by their channel_ids.
   */
  tree_id?:
    | number[]
    | undefined;
  /** The UTF-8 encoded message. May be HTML if the server allows. */
  message?: string | undefined;
}

export interface PermissionDenied {
  /** The denied permission when type is Permission. */
  permission?:
    | number
    | undefined;
  /**
   * channel_id for the channel where the permission was denied when type is
   * Permission.
   */
  channel_id?:
    | number
    | undefined;
  /** The user who was denied permissions, identified by session. */
  session?:
    | number
    | undefined;
  /** Textual reason for the denial. */
  reason?:
    | string
    | undefined;
  /** Type of the denial. */
  type?:
    | PermissionDenied_DenyType
    | undefined;
  /** The name that is invalid when type is UserName. */
  name?: string | undefined;
}

export enum PermissionDenied_DenyType {
  /** Text - Operation denied for other reason, see reason field. */
  Text = 0,
  /** Permission - Permissions were denied. */
  Permission = 1,
  /** SuperUser - Cannot modify SuperUser. */
  SuperUser = 2,
  /** ChannelName - Invalid channel name. */
  ChannelName = 3,
  /** TextTooLong - Text message too long. */
  TextTooLong = 4,
  /** H9K - The flux capacitor was spelled wrong. */
  H9K = 5,
  /** TemporaryChannel - Operation not permitted in temporary channel. */
  TemporaryChannel = 6,
  /** MissingCertificate - Operation requires certificate. */
  MissingCertificate = 7,
  /** UserName - Invalid username. */
  UserName = 8,
  /** ChannelFull - Channel is full. */
  ChannelFull = 9,
  /** NestingLimit - Channels are nested too deeply. */
  NestingLimit = 10,
  /** ChannelCountLimit - Maximum channel count reached. */
  ChannelCountLimit = 11,
  /** ChannelListenerLimit - Amount of listener objects for this channel has been reached */
  ChannelListenerLimit = 12,
  /** UserListenerLimit - Amount of listener proxies for the user has been reached */
  UserListenerLimit = 13,
  UNRECOGNIZED = -1,
}

export function permissionDenied_DenyTypeFromJSON(object: any): PermissionDenied_DenyType {
  switch (object) {
    case 0:
    case "Text":
      return PermissionDenied_DenyType.Text;
    case 1:
    case "Permission":
      return PermissionDenied_DenyType.Permission;
    case 2:
    case "SuperUser":
      return PermissionDenied_DenyType.SuperUser;
    case 3:
    case "ChannelName":
      return PermissionDenied_DenyType.ChannelName;
    case 4:
    case "TextTooLong":
      return PermissionDenied_DenyType.TextTooLong;
    case 5:
    case "H9K":
      return PermissionDenied_DenyType.H9K;
    case 6:
    case "TemporaryChannel":
      return PermissionDenied_DenyType.TemporaryChannel;
    case 7:
    case "MissingCertificate":
      return PermissionDenied_DenyType.MissingCertificate;
    case 8:
    case "UserName":
      return PermissionDenied_DenyType.UserName;
    case 9:
    case "ChannelFull":
      return PermissionDenied_DenyType.ChannelFull;
    case 10:
    case "NestingLimit":
      return PermissionDenied_DenyType.NestingLimit;
    case 11:
    case "ChannelCountLimit":
      return PermissionDenied_DenyType.ChannelCountLimit;
    case 12:
    case "ChannelListenerLimit":
      return PermissionDenied_DenyType.ChannelListenerLimit;
    case 13:
    case "UserListenerLimit":
      return PermissionDenied_DenyType.UserListenerLimit;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PermissionDenied_DenyType.UNRECOGNIZED;
  }
}

export function permissionDenied_DenyTypeToJSON(object: PermissionDenied_DenyType): string {
  switch (object) {
    case PermissionDenied_DenyType.Text:
      return "Text";
    case PermissionDenied_DenyType.Permission:
      return "Permission";
    case PermissionDenied_DenyType.SuperUser:
      return "SuperUser";
    case PermissionDenied_DenyType.ChannelName:
      return "ChannelName";
    case PermissionDenied_DenyType.TextTooLong:
      return "TextTooLong";
    case PermissionDenied_DenyType.H9K:
      return "H9K";
    case PermissionDenied_DenyType.TemporaryChannel:
      return "TemporaryChannel";
    case PermissionDenied_DenyType.MissingCertificate:
      return "MissingCertificate";
    case PermissionDenied_DenyType.UserName:
      return "UserName";
    case PermissionDenied_DenyType.ChannelFull:
      return "ChannelFull";
    case PermissionDenied_DenyType.NestingLimit:
      return "NestingLimit";
    case PermissionDenied_DenyType.ChannelCountLimit:
      return "ChannelCountLimit";
    case PermissionDenied_DenyType.ChannelListenerLimit:
      return "ChannelListenerLimit";
    case PermissionDenied_DenyType.UserListenerLimit:
      return "UserListenerLimit";
    case PermissionDenied_DenyType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ACL {
  /** Channel ID of the channel this message affects. */
  channel_id?:
    | number
    | undefined;
  /** True if the channel inherits its parent's ACLs. */
  inherit_acls?:
    | boolean
    | undefined;
  /** User group specifications. */
  groups?:
    | ACL_ChanGroup[]
    | undefined;
  /** ACL specifications. */
  acls?:
    | ACL_ChanACL[]
    | undefined;
  /** True if the message is a query for ACLs instead of setting them. */
  query?: boolean | undefined;
}

export interface ACL_ChanGroup {
  /** Name of the channel group, UTF-8 encoded. */
  name?:
    | string
    | undefined;
  /** True if the group has been inherited from the parent (Read only). */
  inherited?:
    | boolean
    | undefined;
  /** True if the group members are inherited. */
  inherit?:
    | boolean
    | undefined;
  /** True if the group can be inherited by sub channels. */
  inheritable?:
    | boolean
    | undefined;
  /** Users explicitly included in this group, identified by user_id. */
  add?:
    | number[]
    | undefined;
  /**
   * Users explicitly removed from this group in this channel if the group
   * has been inherited, identified by user_id.
   */
  remove?:
    | number[]
    | undefined;
  /** Users inherited, identified by user_id. */
  inherited_members?: number[] | undefined;
}

export interface ACL_ChanACL {
  /** True if this ACL applies to the current channel. */
  apply_here?:
    | boolean
    | undefined;
  /** True if this ACL applies to the sub channels. */
  apply_subs?:
    | boolean
    | undefined;
  /** True if the ACL has been inherited from the parent. */
  inherited?:
    | boolean
    | undefined;
  /** ID of the user that is affected by this ACL. */
  user_id?:
    | number
    | undefined;
  /** ID of the group that is affected by this ACL. */
  group?:
    | string
    | undefined;
  /** Bit flag field of the permissions granted by this ACL. */
  grant?:
    | number
    | undefined;
  /** Bit flag field of the permissions denied by this ACL. */
  deny?: number | undefined;
}

/**
 * Client may use this message to refresh its registered user information. The
 * client should fill the IDs or Names of the users it wants to refresh. The
 * server fills the missing parts and sends the message back.
 */
export interface QueryUsers {
  /** user_ids. */
  ids?:
    | number[]
    | undefined;
  /** User names in the same order as ids. */
  names?: string[] | undefined;
}

/**
 * Used to initialize and resync the UDP encryption. Either side may request a
 * resync by sending the message without any values filled. The resync is
 * performed by sending the message with only the client or server nonce
 * filled.
 */
export interface CryptSetup {
  /** Encryption key. */
  key?:
    | Buffer
    | undefined;
  /** Client nonce. */
  client_nonce?:
    | Buffer
    | undefined;
  /** Server nonce. */
  server_nonce?: Buffer | undefined;
}

/** Used to add or remove custom context menu item on client-side. */
export interface ContextActionModify {
  /** The action identifier. Used later to initiate an action. */
  action?:
    | string
    | undefined;
  /** The display name of the action. */
  text?:
    | string
    | undefined;
  /**
   * Context bit flags defining where the action should be displayed.
   * Flags can be OR-ed to combine different types.
   */
  context?:
    | number
    | undefined;
  /**
   * Choose either to add or to remove the context action.
   * Note: This field only exists after Mumble 1.2.4-beta1 release.
   *       The message will be recognized as Add regardless of this field
   *       before said release.
   */
  operation?: ContextActionModify_Operation | undefined;
}

export enum ContextActionModify_Context {
  /** Server - Action is applicable to the server. */
  Server = 1,
  /** Channel - Action can target a Channel. */
  Channel = 2,
  /** User - Action can target a User. */
  User = 4,
  UNRECOGNIZED = -1,
}

export function contextActionModify_ContextFromJSON(object: any): ContextActionModify_Context {
  switch (object) {
    case 1:
    case "Server":
      return ContextActionModify_Context.Server;
    case 2:
    case "Channel":
      return ContextActionModify_Context.Channel;
    case 4:
    case "User":
      return ContextActionModify_Context.User;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ContextActionModify_Context.UNRECOGNIZED;
  }
}

export function contextActionModify_ContextToJSON(object: ContextActionModify_Context): string {
  switch (object) {
    case ContextActionModify_Context.Server:
      return "Server";
    case ContextActionModify_Context.Channel:
      return "Channel";
    case ContextActionModify_Context.User:
      return "User";
    case ContextActionModify_Context.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ContextActionModify_Operation {
  Add = 0,
  Remove = 1,
  UNRECOGNIZED = -1,
}

export function contextActionModify_OperationFromJSON(object: any): ContextActionModify_Operation {
  switch (object) {
    case 0:
    case "Add":
      return ContextActionModify_Operation.Add;
    case 1:
    case "Remove":
      return ContextActionModify_Operation.Remove;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ContextActionModify_Operation.UNRECOGNIZED;
  }
}

export function contextActionModify_OperationToJSON(object: ContextActionModify_Operation): string {
  switch (object) {
    case ContextActionModify_Operation.Add:
      return "Add";
    case ContextActionModify_Operation.Remove:
      return "Remove";
    case ContextActionModify_Operation.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Sent by the client when it wants to initiate a Context action. */
export interface ContextAction {
  /** The target User for the action, identified by session. */
  session?:
    | number
    | undefined;
  /** The target Channel for the action, identified by channel_id. */
  channel_id?:
    | number
    | undefined;
  /** The action that should be executed. */
  action?: string | undefined;
}

/** Lists the registered users. */
export interface UserList {
  /** A list of registered users. */
  users?: UserList_User[] | undefined;
}

export interface UserList_User {
  /** Registered user ID. */
  user_id?:
    | number
    | undefined;
  /** Registered user name. */
  name?: string | undefined;
  last_seen?: string | undefined;
  last_channel?: number | undefined;
}

/**
 * Sent by the client when it wants to register or clear whisper targets.
 *
 * Note: The first available target ID is 1 as 0 is reserved for normal
 * talking. Maximum target ID is 30.
 */
export interface VoiceTarget {
  /** Voice target ID. */
  id?:
    | number
    | undefined;
  /** The receivers that this voice target includes. */
  targets?: VoiceTarget_Target[] | undefined;
}

export interface VoiceTarget_Target {
  /** Users that are included as targets. */
  session?:
    | number[]
    | undefined;
  /** Channel that is included as a target. */
  channel_id?:
    | number
    | undefined;
  /** ACL group that is included as a target. */
  group?:
    | string
    | undefined;
  /** True if the voice should follow links from the specified channel. */
  links?:
    | boolean
    | undefined;
  /**
   * True if the voice should also be sent to children of the specific
   * channel.
   */
  children?: boolean | undefined;
}

/**
 * Sent by the client when it wants permissions for a certain channel. Sent by
 * the server when it replies to the query or wants the user to resync all
 * channel permissions.
 */
export interface PermissionQuery {
  /** channel_id of the channel for which the permissions are queried. */
  channel_id?:
    | number
    | undefined;
  /** Channel permissions. */
  permissions?:
    | number
    | undefined;
  /**
   * True if the client should drop its current permission information for all
   * channels.
   */
  flush?: boolean | undefined;
}

/**
 * Sent by the server to notify the users of the version of the CELT codec they
 * should use. This may change during the connection when new users join.
 */
export interface CodecVersion {
  /** The version of the CELT Alpha codec. */
  alpha?:
    | number
    | undefined;
  /** The version of the CELT Beta codec. */
  beta?:
    | number
    | undefined;
  /** True if the user should prefer Alpha over Beta. */
  prefer_alpha?: boolean | undefined;
  opus?: boolean | undefined;
}

/** Used to communicate user stats between the server and clients. */
export interface UserStats {
  /** User whose stats these are. */
  session?:
    | number
    | undefined;
  /** True if the message contains only mutable stats (packets, ping). */
  stats_only?:
    | boolean
    | undefined;
  /** Full user certificate chain of the user certificate in DER format. */
  certificates?:
    | Buffer[]
    | undefined;
  /** Packet statistics for packets received from the client. */
  from_client?:
    | UserStats_Stats
    | undefined;
  /** Packet statistics for packets sent by the server. */
  from_server?:
    | UserStats_Stats
    | undefined;
  /** Amount of UDP packets sent. */
  udp_packets?:
    | number
    | undefined;
  /** Amount of TCP packets sent. */
  tcp_packets?:
    | number
    | undefined;
  /** UDP ping average. */
  udp_ping_avg?:
    | number
    | undefined;
  /** UDP ping variance. */
  udp_ping_var?:
    | number
    | undefined;
  /** TCP ping average. */
  tcp_ping_avg?:
    | number
    | undefined;
  /** TCP ping variance. */
  tcp_ping_var?:
    | number
    | undefined;
  /** Client version. */
  version?:
    | Version
    | undefined;
  /**
   * A list of CELT bitstream version constants supported by the client of this
   * user.
   */
  celt_versions?:
    | number[]
    | undefined;
  /** Client IP address. */
  address?:
    | Buffer
    | undefined;
  /** Bandwidth used by this client. */
  bandwidth?:
    | number
    | undefined;
  /** Connection duration. */
  onlinesecs?:
    | number
    | undefined;
  /** Duration since last activity. */
  idlesecs?:
    | number
    | undefined;
  /** True if the user has a strong certificate. */
  strong_certificate?: boolean | undefined;
  opus?: boolean | undefined;
}

export interface UserStats_Stats {
  /** The amount of good packets received. */
  good?:
    | number
    | undefined;
  /** The amount of late packets received. */
  late?:
    | number
    | undefined;
  /** The amount of packets never received. */
  lost?:
    | number
    | undefined;
  /** The amount of nonce resyncs. */
  resync?: number | undefined;
}

/**
 * Used by the client to request binary data from the server. By default large
 * comments or textures are not sent within standard messages but instead the
 * hash is. If the client does not recognize the hash it may request the
 * resource when it needs it. The client does so by sending a RequestBlob
 * message with the correct fields filled with the user sessions or channel_ids
 * it wants to receive. The server replies to this by sending a new
 * UserState/ChannelState message with the resources filled even if they would
 * normally be transmitted as hashes.
 */
export interface RequestBlob {
  /** sessions of the requested UserState textures. */
  session_texture?:
    | number[]
    | undefined;
  /** sessions of the requested UserState comments. */
  session_comment?:
    | number[]
    | undefined;
  /** channel_ids of the requested ChannelState descriptions. */
  channel_description?: number[] | undefined;
}

/**
 * Sent by the server when it informs the clients on server configuration
 * details.
 */
export interface ServerConfig {
  /** The maximum bandwidth the clients should use. */
  max_bandwidth?:
    | number
    | undefined;
  /** Server welcome text. */
  welcome_text?:
    | string
    | undefined;
  /** True if the server allows HTML. */
  allow_html?:
    | boolean
    | undefined;
  /** Maximum text message length. */
  message_length?:
    | number
    | undefined;
  /** Maximum image message length. */
  image_message_length?:
    | number
    | undefined;
  /** The maximum number of users allowed on the server. */
  max_users?:
    | number
    | undefined;
  /** Whether using Mumble's recording feature is allowed on the server */
  recording_allowed?: boolean | undefined;
}

/**
 * Sent by the server to inform the clients of suggested client configuration
 * specified by the server administrator.
 */
export interface SuggestConfig {
  /** Suggested client version. */
  version?:
    | number
    | undefined;
  /**
   * True if the administrator suggests positional audio to be used on this
   * server.
   */
  positional?:
    | boolean
    | undefined;
  /** True if the administrator suggests push to talk to be used on this server. */
  push_to_talk?: boolean | undefined;
}

/** Used to send plugin messages between clients */
export interface PluginDataTransmission {
  /** The session ID of the client this message was sent from */
  senderSession?:
    | number
    | undefined;
  /** The session IDs of the clients that should receive this message */
  receiverSessions?:
    | number[]
    | undefined;
  /** The data that is sent */
  data?:
    | Buffer
    | undefined;
  /**
   * The ID of the sent data. This will be used by plugins to check whether they will
   * process it or not
   */
  dataID?: string | undefined;
}

function createBaseVersion(): Version {
  return { version: undefined, release: undefined, os: undefined, os_version: undefined };
}

export const Version: MessageFns<Version> = {
  encode(message: Version, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.release !== undefined) {
      writer.uint32(18).string(message.release);
    }
    if (message.os !== undefined) {
      writer.uint32(26).string(message.os);
    }
    if (message.os_version !== undefined) {
      writer.uint32(34).string(message.os_version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Version {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.release = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.os = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.os_version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Version {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : undefined,
      release: isSet(object.release) ? globalThis.String(object.release) : undefined,
      os: isSet(object.os) ? globalThis.String(object.os) : undefined,
      os_version: isSet(object.os_version) ? globalThis.String(object.os_version) : undefined,
    };
  },

  toJSON(message: Version): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = Math.round(message.version);
    }
    if (message.release !== undefined) {
      obj.release = message.release;
    }
    if (message.os !== undefined) {
      obj.os = message.os;
    }
    if (message.os_version !== undefined) {
      obj.os_version = message.os_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Version>, I>>(base?: I): Version {
    return Version.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Version>, I>>(object: I): Version {
    const message = createBaseVersion();
    message.version = object.version ?? undefined;
    message.release = object.release ?? undefined;
    message.os = object.os ?? undefined;
    message.os_version = object.os_version ?? undefined;
    return message;
  },
};

function createBaseUDPTunnel(): UDPTunnel {
  return { packet: undefined };
}

export const UDPTunnel: MessageFns<UDPTunnel> = {
  encode(message: UDPTunnel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.packet !== undefined) {
      writer.uint32(10).bytes(message.packet);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UDPTunnel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUDPTunnel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.packet = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UDPTunnel {
    return { packet: isSet(object.packet) ? Buffer.from(bytesFromBase64(object.packet)) : undefined };
  },

  toJSON(message: UDPTunnel): unknown {
    const obj: any = {};
    if (message.packet !== undefined) {
      obj.packet = base64FromBytes(message.packet);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UDPTunnel>, I>>(base?: I): UDPTunnel {
    return UDPTunnel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UDPTunnel>, I>>(object: I): UDPTunnel {
    const message = createBaseUDPTunnel();
    message.packet = object.packet ?? undefined;
    return message;
  },
};

function createBaseAuthenticate(): Authenticate {
  return { username: undefined, password: undefined, tokens: [], celt_versions: [], opus: undefined };
}

export const Authenticate: MessageFns<Authenticate> = {
  encode(message: Authenticate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== undefined) {
      writer.uint32(10).string(message.username);
    }
    if (message.password !== undefined) {
      writer.uint32(18).string(message.password);
    }
    if (message.tokens !== undefined && message.tokens.length !== 0) {
      for (const v of message.tokens) {
        writer.uint32(26).string(v!);
      }
    }
    if (message.celt_versions !== undefined && message.celt_versions.length !== 0) {
      for (const v of message.celt_versions) {
        writer.uint32(32).int32(v!);
      }
    }
    if (message.opus !== undefined) {
      writer.uint32(40).bool(message.opus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Authenticate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.tokens!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.celt_versions!.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.celt_versions!.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.opus = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Authenticate {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : undefined,
      password: isSet(object.password) ? globalThis.String(object.password) : undefined,
      tokens: globalThis.Array.isArray(object?.tokens) ? object.tokens.map((e: any) => globalThis.String(e)) : [],
      celt_versions: globalThis.Array.isArray(object?.celt_versions)
        ? object.celt_versions.map((e: any) => globalThis.Number(e))
        : [],
      opus: isSet(object.opus) ? globalThis.Boolean(object.opus) : undefined,
    };
  },

  toJSON(message: Authenticate): unknown {
    const obj: any = {};
    if (message.username !== undefined) {
      obj.username = message.username;
    }
    if (message.password !== undefined) {
      obj.password = message.password;
    }
    if (message.tokens?.length) {
      obj.tokens = message.tokens;
    }
    if (message.celt_versions?.length) {
      obj.celt_versions = message.celt_versions.map((e) => Math.round(e));
    }
    if (message.opus !== undefined) {
      obj.opus = message.opus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Authenticate>, I>>(base?: I): Authenticate {
    return Authenticate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Authenticate>, I>>(object: I): Authenticate {
    const message = createBaseAuthenticate();
    message.username = object.username ?? undefined;
    message.password = object.password ?? undefined;
    message.tokens = object.tokens?.map((e) => e) || [];
    message.celt_versions = object.celt_versions?.map((e) => e) || [];
    message.opus = object.opus ?? undefined;
    return message;
  },
};

function createBasePing(): Ping {
  return {
    timestamp: undefined,
    good: undefined,
    late: undefined,
    lost: undefined,
    resync: undefined,
    udp_packets: undefined,
    tcp_packets: undefined,
    udp_ping_avg: undefined,
    udp_ping_var: undefined,
    tcp_ping_avg: undefined,
    tcp_ping_var: undefined,
  };
}

export const Ping: MessageFns<Ping> = {
  encode(message: Ping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined) {
      writer.uint32(8).uint64(message.timestamp);
    }
    if (message.good !== undefined) {
      writer.uint32(16).uint32(message.good);
    }
    if (message.late !== undefined) {
      writer.uint32(24).uint32(message.late);
    }
    if (message.lost !== undefined) {
      writer.uint32(32).uint32(message.lost);
    }
    if (message.resync !== undefined) {
      writer.uint32(40).uint32(message.resync);
    }
    if (message.udp_packets !== undefined) {
      writer.uint32(48).uint32(message.udp_packets);
    }
    if (message.tcp_packets !== undefined) {
      writer.uint32(56).uint32(message.tcp_packets);
    }
    if (message.udp_ping_avg !== undefined) {
      writer.uint32(69).float(message.udp_ping_avg);
    }
    if (message.udp_ping_var !== undefined) {
      writer.uint32(77).float(message.udp_ping_var);
    }
    if (message.tcp_ping_avg !== undefined) {
      writer.uint32(85).float(message.tcp_ping_avg);
    }
    if (message.tcp_ping_var !== undefined) {
      writer.uint32(93).float(message.tcp_ping_var);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestamp = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.good = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.late = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lost = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.resync = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.udp_packets = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tcp_packets = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.udp_ping_avg = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.udp_ping_var = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 85) {
            break;
          }

          message.tcp_ping_avg = reader.float();
          continue;
        }
        case 11: {
          if (tag !== 93) {
            break;
          }

          message.tcp_ping_var = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ping {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : undefined,
      good: isSet(object.good) ? globalThis.Number(object.good) : undefined,
      late: isSet(object.late) ? globalThis.Number(object.late) : undefined,
      lost: isSet(object.lost) ? globalThis.Number(object.lost) : undefined,
      resync: isSet(object.resync) ? globalThis.Number(object.resync) : undefined,
      udp_packets: isSet(object.udp_packets) ? globalThis.Number(object.udp_packets) : undefined,
      tcp_packets: isSet(object.tcp_packets) ? globalThis.Number(object.tcp_packets) : undefined,
      udp_ping_avg: isSet(object.udp_ping_avg) ? globalThis.Number(object.udp_ping_avg) : undefined,
      udp_ping_var: isSet(object.udp_ping_var) ? globalThis.Number(object.udp_ping_var) : undefined,
      tcp_ping_avg: isSet(object.tcp_ping_avg) ? globalThis.Number(object.tcp_ping_avg) : undefined,
      tcp_ping_var: isSet(object.tcp_ping_var) ? globalThis.Number(object.tcp_ping_var) : undefined,
    };
  },

  toJSON(message: Ping): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.good !== undefined) {
      obj.good = Math.round(message.good);
    }
    if (message.late !== undefined) {
      obj.late = Math.round(message.late);
    }
    if (message.lost !== undefined) {
      obj.lost = Math.round(message.lost);
    }
    if (message.resync !== undefined) {
      obj.resync = Math.round(message.resync);
    }
    if (message.udp_packets !== undefined) {
      obj.udp_packets = Math.round(message.udp_packets);
    }
    if (message.tcp_packets !== undefined) {
      obj.tcp_packets = Math.round(message.tcp_packets);
    }
    if (message.udp_ping_avg !== undefined) {
      obj.udp_ping_avg = message.udp_ping_avg;
    }
    if (message.udp_ping_var !== undefined) {
      obj.udp_ping_var = message.udp_ping_var;
    }
    if (message.tcp_ping_avg !== undefined) {
      obj.tcp_ping_avg = message.tcp_ping_avg;
    }
    if (message.tcp_ping_var !== undefined) {
      obj.tcp_ping_var = message.tcp_ping_var;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Ping>, I>>(base?: I): Ping {
    return Ping.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Ping>, I>>(object: I): Ping {
    const message = createBasePing();
    message.timestamp = object.timestamp ?? undefined;
    message.good = object.good ?? undefined;
    message.late = object.late ?? undefined;
    message.lost = object.lost ?? undefined;
    message.resync = object.resync ?? undefined;
    message.udp_packets = object.udp_packets ?? undefined;
    message.tcp_packets = object.tcp_packets ?? undefined;
    message.udp_ping_avg = object.udp_ping_avg ?? undefined;
    message.udp_ping_var = object.udp_ping_var ?? undefined;
    message.tcp_ping_avg = object.tcp_ping_avg ?? undefined;
    message.tcp_ping_var = object.tcp_ping_var ?? undefined;
    return message;
  },
};

function createBaseReject(): Reject {
  return { type: undefined, reason: undefined };
}

export const Reject: MessageFns<Reject> = {
  encode(message: Reject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== undefined) {
      writer.uint32(8).int32(message.type);
    }
    if (message.reason !== undefined) {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Reject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reject {
    return {
      type: isSet(object.type) ? reject_RejectTypeFromJSON(object.type) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
    };
  },

  toJSON(message: Reject): unknown {
    const obj: any = {};
    if (message.type !== undefined) {
      obj.type = reject_RejectTypeToJSON(message.type);
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Reject>, I>>(base?: I): Reject {
    return Reject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Reject>, I>>(object: I): Reject {
    const message = createBaseReject();
    message.type = object.type ?? undefined;
    message.reason = object.reason ?? undefined;
    return message;
  },
};

function createBaseServerSync(): ServerSync {
  return { session: undefined, max_bandwidth: undefined, welcome_text: undefined, permissions: undefined };
}

export const ServerSync: MessageFns<ServerSync> = {
  encode(message: ServerSync, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      writer.uint32(8).uint32(message.session);
    }
    if (message.max_bandwidth !== undefined) {
      writer.uint32(16).uint32(message.max_bandwidth);
    }
    if (message.welcome_text !== undefined) {
      writer.uint32(26).string(message.welcome_text);
    }
    if (message.permissions !== undefined) {
      writer.uint32(32).uint64(message.permissions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerSync {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerSync();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.session = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.max_bandwidth = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.welcome_text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.permissions = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerSync {
    return {
      session: isSet(object.session) ? globalThis.Number(object.session) : undefined,
      max_bandwidth: isSet(object.max_bandwidth) ? globalThis.Number(object.max_bandwidth) : undefined,
      welcome_text: isSet(object.welcome_text) ? globalThis.String(object.welcome_text) : undefined,
      permissions: isSet(object.permissions) ? globalThis.Number(object.permissions) : undefined,
    };
  },

  toJSON(message: ServerSync): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = Math.round(message.session);
    }
    if (message.max_bandwidth !== undefined) {
      obj.max_bandwidth = Math.round(message.max_bandwidth);
    }
    if (message.welcome_text !== undefined) {
      obj.welcome_text = message.welcome_text;
    }
    if (message.permissions !== undefined) {
      obj.permissions = Math.round(message.permissions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerSync>, I>>(base?: I): ServerSync {
    return ServerSync.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerSync>, I>>(object: I): ServerSync {
    const message = createBaseServerSync();
    message.session = object.session ?? undefined;
    message.max_bandwidth = object.max_bandwidth ?? undefined;
    message.welcome_text = object.welcome_text ?? undefined;
    message.permissions = object.permissions ?? undefined;
    return message;
  },
};

function createBaseChannelRemove(): ChannelRemove {
  return { channel_id: undefined };
}

export const ChannelRemove: MessageFns<ChannelRemove> = {
  encode(message: ChannelRemove, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel_id !== undefined) {
      writer.uint32(8).uint32(message.channel_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChannelRemove {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChannelRemove();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChannelRemove {
    return { channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined };
  },

  toJSON(message: ChannelRemove): unknown {
    const obj: any = {};
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelRemove>, I>>(base?: I): ChannelRemove {
    return ChannelRemove.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelRemove>, I>>(object: I): ChannelRemove {
    const message = createBaseChannelRemove();
    message.channel_id = object.channel_id ?? undefined;
    return message;
  },
};

function createBaseChannelState(): ChannelState {
  return {
    channel_id: undefined,
    parent: undefined,
    name: undefined,
    links: [],
    description: undefined,
    links_add: [],
    links_remove: [],
    temporary: undefined,
    position: undefined,
    description_hash: undefined,
    max_users: undefined,
    is_enter_restricted: undefined,
    can_enter: undefined,
  };
}

export const ChannelState: MessageFns<ChannelState> = {
  encode(message: ChannelState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel_id !== undefined) {
      writer.uint32(8).uint32(message.channel_id);
    }
    if (message.parent !== undefined) {
      writer.uint32(16).uint32(message.parent);
    }
    if (message.name !== undefined) {
      writer.uint32(26).string(message.name);
    }
    if (message.links !== undefined && message.links.length !== 0) {
      for (const v of message.links) {
        writer.uint32(32).uint32(v!);
      }
    }
    if (message.description !== undefined) {
      writer.uint32(42).string(message.description);
    }
    if (message.links_add !== undefined && message.links_add.length !== 0) {
      for (const v of message.links_add) {
        writer.uint32(48).uint32(v!);
      }
    }
    if (message.links_remove !== undefined && message.links_remove.length !== 0) {
      for (const v of message.links_remove) {
        writer.uint32(56).uint32(v!);
      }
    }
    if (message.temporary !== undefined) {
      writer.uint32(64).bool(message.temporary);
    }
    if (message.position !== undefined) {
      writer.uint32(72).int32(message.position);
    }
    if (message.description_hash !== undefined) {
      writer.uint32(82).bytes(message.description_hash);
    }
    if (message.max_users !== undefined) {
      writer.uint32(88).uint32(message.max_users);
    }
    if (message.is_enter_restricted !== undefined) {
      writer.uint32(96).bool(message.is_enter_restricted);
    }
    if (message.can_enter !== undefined) {
      writer.uint32(104).bool(message.can_enter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChannelState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChannelState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.parent = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.links!.push(reader.uint32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.links!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.links_add!.push(reader.uint32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.links_add!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag === 56) {
            message.links_remove!.push(reader.uint32());

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.links_remove!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.temporary = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.position = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.description_hash = Buffer.from(reader.bytes());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.max_users = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.is_enter_restricted = reader.bool();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.can_enter = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChannelState {
    return {
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      parent: isSet(object.parent) ? globalThis.Number(object.parent) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      links: globalThis.Array.isArray(object?.links) ? object.links.map((e: any) => globalThis.Number(e)) : [],
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      links_add: globalThis.Array.isArray(object?.links_add)
        ? object.links_add.map((e: any) => globalThis.Number(e))
        : [],
      links_remove: globalThis.Array.isArray(object?.links_remove)
        ? object.links_remove.map((e: any) => globalThis.Number(e))
        : [],
      temporary: isSet(object.temporary) ? globalThis.Boolean(object.temporary) : undefined,
      position: isSet(object.position) ? globalThis.Number(object.position) : undefined,
      description_hash: isSet(object.description_hash)
        ? Buffer.from(bytesFromBase64(object.description_hash))
        : undefined,
      max_users: isSet(object.max_users) ? globalThis.Number(object.max_users) : undefined,
      is_enter_restricted: isSet(object.is_enter_restricted)
        ? globalThis.Boolean(object.is_enter_restricted)
        : undefined,
      can_enter: isSet(object.can_enter) ? globalThis.Boolean(object.can_enter) : undefined,
    };
  },

  toJSON(message: ChannelState): unknown {
    const obj: any = {};
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.parent !== undefined) {
      obj.parent = Math.round(message.parent);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.links?.length) {
      obj.links = message.links.map((e) => Math.round(e));
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.links_add?.length) {
      obj.links_add = message.links_add.map((e) => Math.round(e));
    }
    if (message.links_remove?.length) {
      obj.links_remove = message.links_remove.map((e) => Math.round(e));
    }
    if (message.temporary !== undefined) {
      obj.temporary = message.temporary;
    }
    if (message.position !== undefined) {
      obj.position = Math.round(message.position);
    }
    if (message.description_hash !== undefined) {
      obj.description_hash = base64FromBytes(message.description_hash);
    }
    if (message.max_users !== undefined) {
      obj.max_users = Math.round(message.max_users);
    }
    if (message.is_enter_restricted !== undefined) {
      obj.is_enter_restricted = message.is_enter_restricted;
    }
    if (message.can_enter !== undefined) {
      obj.can_enter = message.can_enter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelState>, I>>(base?: I): ChannelState {
    return ChannelState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelState>, I>>(object: I): ChannelState {
    const message = createBaseChannelState();
    message.channel_id = object.channel_id ?? undefined;
    message.parent = object.parent ?? undefined;
    message.name = object.name ?? undefined;
    message.links = object.links?.map((e) => e) || [];
    message.description = object.description ?? undefined;
    message.links_add = object.links_add?.map((e) => e) || [];
    message.links_remove = object.links_remove?.map((e) => e) || [];
    message.temporary = object.temporary ?? undefined;
    message.position = object.position ?? undefined;
    message.description_hash = object.description_hash ?? undefined;
    message.max_users = object.max_users ?? undefined;
    message.is_enter_restricted = object.is_enter_restricted ?? undefined;
    message.can_enter = object.can_enter ?? undefined;
    return message;
  },
};

function createBaseUserRemove(): UserRemove {
  return { session: undefined, actor: undefined, reason: undefined, ban: undefined };
}

export const UserRemove: MessageFns<UserRemove> = {
  encode(message: UserRemove, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      writer.uint32(8).uint32(message.session);
    }
    if (message.actor !== undefined) {
      writer.uint32(16).uint32(message.actor);
    }
    if (message.reason !== undefined) {
      writer.uint32(26).string(message.reason);
    }
    if (message.ban !== undefined) {
      writer.uint32(32).bool(message.ban);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserRemove {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserRemove();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.session = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.actor = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.ban = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserRemove {
    return {
      session: isSet(object.session) ? globalThis.Number(object.session) : undefined,
      actor: isSet(object.actor) ? globalThis.Number(object.actor) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
      ban: isSet(object.ban) ? globalThis.Boolean(object.ban) : undefined,
    };
  },

  toJSON(message: UserRemove): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = Math.round(message.session);
    }
    if (message.actor !== undefined) {
      obj.actor = Math.round(message.actor);
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    if (message.ban !== undefined) {
      obj.ban = message.ban;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserRemove>, I>>(base?: I): UserRemove {
    return UserRemove.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserRemove>, I>>(object: I): UserRemove {
    const message = createBaseUserRemove();
    message.session = object.session ?? undefined;
    message.actor = object.actor ?? undefined;
    message.reason = object.reason ?? undefined;
    message.ban = object.ban ?? undefined;
    return message;
  },
};

function createBaseUserState(): UserState {
  return {
    session: undefined,
    actor: undefined,
    name: undefined,
    user_id: undefined,
    channel_id: undefined,
    mute: undefined,
    deaf: undefined,
    suppress: undefined,
    self_mute: undefined,
    self_deaf: undefined,
    texture: undefined,
    plugin_context: undefined,
    plugin_identity: undefined,
    comment: undefined,
    hash: undefined,
    comment_hash: undefined,
    texture_hash: undefined,
    priority_speaker: undefined,
    recording: undefined,
    temporary_access_tokens: [],
    listening_channel_add: [],
    listening_channel_remove: [],
  };
}

export const UserState: MessageFns<UserState> = {
  encode(message: UserState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      writer.uint32(8).uint32(message.session);
    }
    if (message.actor !== undefined) {
      writer.uint32(16).uint32(message.actor);
    }
    if (message.name !== undefined) {
      writer.uint32(26).string(message.name);
    }
    if (message.user_id !== undefined) {
      writer.uint32(32).uint32(message.user_id);
    }
    if (message.channel_id !== undefined) {
      writer.uint32(40).uint32(message.channel_id);
    }
    if (message.mute !== undefined) {
      writer.uint32(48).bool(message.mute);
    }
    if (message.deaf !== undefined) {
      writer.uint32(56).bool(message.deaf);
    }
    if (message.suppress !== undefined) {
      writer.uint32(64).bool(message.suppress);
    }
    if (message.self_mute !== undefined) {
      writer.uint32(72).bool(message.self_mute);
    }
    if (message.self_deaf !== undefined) {
      writer.uint32(80).bool(message.self_deaf);
    }
    if (message.texture !== undefined) {
      writer.uint32(90).bytes(message.texture);
    }
    if (message.plugin_context !== undefined) {
      writer.uint32(98).bytes(message.plugin_context);
    }
    if (message.plugin_identity !== undefined) {
      writer.uint32(106).string(message.plugin_identity);
    }
    if (message.comment !== undefined) {
      writer.uint32(114).string(message.comment);
    }
    if (message.hash !== undefined) {
      writer.uint32(122).string(message.hash);
    }
    if (message.comment_hash !== undefined) {
      writer.uint32(130).bytes(message.comment_hash);
    }
    if (message.texture_hash !== undefined) {
      writer.uint32(138).bytes(message.texture_hash);
    }
    if (message.priority_speaker !== undefined) {
      writer.uint32(144).bool(message.priority_speaker);
    }
    if (message.recording !== undefined) {
      writer.uint32(152).bool(message.recording);
    }
    if (message.temporary_access_tokens !== undefined && message.temporary_access_tokens.length !== 0) {
      for (const v of message.temporary_access_tokens) {
        writer.uint32(162).string(v!);
      }
    }
    if (message.listening_channel_add !== undefined && message.listening_channel_add.length !== 0) {
      for (const v of message.listening_channel_add) {
        writer.uint32(168).uint32(v!);
      }
    }
    if (message.listening_channel_remove !== undefined && message.listening_channel_remove.length !== 0) {
      for (const v of message.listening_channel_remove) {
        writer.uint32(176).uint32(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.session = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.actor = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.user_id = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.mute = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.deaf = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.suppress = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.self_mute = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.self_deaf = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.texture = Buffer.from(reader.bytes());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.plugin_context = Buffer.from(reader.bytes());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.plugin_identity = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.comment = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.comment_hash = Buffer.from(reader.bytes());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.texture_hash = Buffer.from(reader.bytes());
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.priority_speaker = reader.bool();
          continue;
        }
        case 19: {
          if (tag !== 152) {
            break;
          }

          message.recording = reader.bool();
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.temporary_access_tokens!.push(el);
          }
          continue;
        }
        case 21: {
          if (tag === 168) {
            message.listening_channel_add!.push(reader.uint32());

            continue;
          }

          if (tag === 170) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.listening_channel_add!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 22: {
          if (tag === 176) {
            message.listening_channel_remove!.push(reader.uint32());

            continue;
          }

          if (tag === 178) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.listening_channel_remove!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserState {
    return {
      session: isSet(object.session) ? globalThis.Number(object.session) : undefined,
      actor: isSet(object.actor) ? globalThis.Number(object.actor) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      user_id: isSet(object.user_id) ? globalThis.Number(object.user_id) : undefined,
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      mute: isSet(object.mute) ? globalThis.Boolean(object.mute) : undefined,
      deaf: isSet(object.deaf) ? globalThis.Boolean(object.deaf) : undefined,
      suppress: isSet(object.suppress) ? globalThis.Boolean(object.suppress) : undefined,
      self_mute: isSet(object.self_mute) ? globalThis.Boolean(object.self_mute) : undefined,
      self_deaf: isSet(object.self_deaf) ? globalThis.Boolean(object.self_deaf) : undefined,
      texture: isSet(object.texture) ? Buffer.from(bytesFromBase64(object.texture)) : undefined,
      plugin_context: isSet(object.plugin_context) ? Buffer.from(bytesFromBase64(object.plugin_context)) : undefined,
      plugin_identity: isSet(object.plugin_identity) ? globalThis.String(object.plugin_identity) : undefined,
      comment: isSet(object.comment) ? globalThis.String(object.comment) : undefined,
      hash: isSet(object.hash) ? globalThis.String(object.hash) : undefined,
      comment_hash: isSet(object.comment_hash) ? Buffer.from(bytesFromBase64(object.comment_hash)) : undefined,
      texture_hash: isSet(object.texture_hash) ? Buffer.from(bytesFromBase64(object.texture_hash)) : undefined,
      priority_speaker: isSet(object.priority_speaker) ? globalThis.Boolean(object.priority_speaker) : undefined,
      recording: isSet(object.recording) ? globalThis.Boolean(object.recording) : undefined,
      temporary_access_tokens: globalThis.Array.isArray(object?.temporary_access_tokens)
        ? object.temporary_access_tokens.map((e: any) => globalThis.String(e))
        : [],
      listening_channel_add: globalThis.Array.isArray(object?.listening_channel_add)
        ? object.listening_channel_add.map((e: any) => globalThis.Number(e))
        : [],
      listening_channel_remove: globalThis.Array.isArray(object?.listening_channel_remove)
        ? object.listening_channel_remove.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: UserState): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = Math.round(message.session);
    }
    if (message.actor !== undefined) {
      obj.actor = Math.round(message.actor);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.user_id !== undefined) {
      obj.user_id = Math.round(message.user_id);
    }
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.mute !== undefined) {
      obj.mute = message.mute;
    }
    if (message.deaf !== undefined) {
      obj.deaf = message.deaf;
    }
    if (message.suppress !== undefined) {
      obj.suppress = message.suppress;
    }
    if (message.self_mute !== undefined) {
      obj.self_mute = message.self_mute;
    }
    if (message.self_deaf !== undefined) {
      obj.self_deaf = message.self_deaf;
    }
    if (message.texture !== undefined) {
      obj.texture = base64FromBytes(message.texture);
    }
    if (message.plugin_context !== undefined) {
      obj.plugin_context = base64FromBytes(message.plugin_context);
    }
    if (message.plugin_identity !== undefined) {
      obj.plugin_identity = message.plugin_identity;
    }
    if (message.comment !== undefined) {
      obj.comment = message.comment;
    }
    if (message.hash !== undefined) {
      obj.hash = message.hash;
    }
    if (message.comment_hash !== undefined) {
      obj.comment_hash = base64FromBytes(message.comment_hash);
    }
    if (message.texture_hash !== undefined) {
      obj.texture_hash = base64FromBytes(message.texture_hash);
    }
    if (message.priority_speaker !== undefined) {
      obj.priority_speaker = message.priority_speaker;
    }
    if (message.recording !== undefined) {
      obj.recording = message.recording;
    }
    if (message.temporary_access_tokens?.length) {
      obj.temporary_access_tokens = message.temporary_access_tokens;
    }
    if (message.listening_channel_add?.length) {
      obj.listening_channel_add = message.listening_channel_add.map((e) => Math.round(e));
    }
    if (message.listening_channel_remove?.length) {
      obj.listening_channel_remove = message.listening_channel_remove.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserState>, I>>(base?: I): UserState {
    return UserState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserState>, I>>(object: I): UserState {
    const message = createBaseUserState();
    message.session = object.session ?? undefined;
    message.actor = object.actor ?? undefined;
    message.name = object.name ?? undefined;
    message.user_id = object.user_id ?? undefined;
    message.channel_id = object.channel_id ?? undefined;
    message.mute = object.mute ?? undefined;
    message.deaf = object.deaf ?? undefined;
    message.suppress = object.suppress ?? undefined;
    message.self_mute = object.self_mute ?? undefined;
    message.self_deaf = object.self_deaf ?? undefined;
    message.texture = object.texture ?? undefined;
    message.plugin_context = object.plugin_context ?? undefined;
    message.plugin_identity = object.plugin_identity ?? undefined;
    message.comment = object.comment ?? undefined;
    message.hash = object.hash ?? undefined;
    message.comment_hash = object.comment_hash ?? undefined;
    message.texture_hash = object.texture_hash ?? undefined;
    message.priority_speaker = object.priority_speaker ?? undefined;
    message.recording = object.recording ?? undefined;
    message.temporary_access_tokens = object.temporary_access_tokens?.map((e) => e) || [];
    message.listening_channel_add = object.listening_channel_add?.map((e) => e) || [];
    message.listening_channel_remove = object.listening_channel_remove?.map((e) => e) || [];
    return message;
  },
};

function createBaseBanList(): BanList {
  return { bans: [], query: undefined };
}

export const BanList: MessageFns<BanList> = {
  encode(message: BanList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bans !== undefined && message.bans.length !== 0) {
      for (const v of message.bans) {
        BanList_BanEntry.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.query !== undefined) {
      writer.uint32(16).bool(message.query);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BanList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBanList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = BanList_BanEntry.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.bans!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.query = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BanList {
    return {
      bans: globalThis.Array.isArray(object?.bans) ? object.bans.map((e: any) => BanList_BanEntry.fromJSON(e)) : [],
      query: isSet(object.query) ? globalThis.Boolean(object.query) : undefined,
    };
  },

  toJSON(message: BanList): unknown {
    const obj: any = {};
    if (message.bans?.length) {
      obj.bans = message.bans.map((e) => BanList_BanEntry.toJSON(e));
    }
    if (message.query !== undefined) {
      obj.query = message.query;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BanList>, I>>(base?: I): BanList {
    return BanList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BanList>, I>>(object: I): BanList {
    const message = createBaseBanList();
    message.bans = object.bans?.map((e) => BanList_BanEntry.fromPartial(e)) || [];
    message.query = object.query ?? undefined;
    return message;
  },
};

function createBaseBanList_BanEntry(): BanList_BanEntry {
  return {
    address: undefined,
    mask: undefined,
    name: undefined,
    hash: undefined,
    reason: undefined,
    start: undefined,
    duration: undefined,
  };
}

export const BanList_BanEntry: MessageFns<BanList_BanEntry> = {
  encode(message: BanList_BanEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== undefined) {
      writer.uint32(10).bytes(message.address);
    }
    if (message.mask !== undefined) {
      writer.uint32(16).uint32(message.mask);
    }
    if (message.name !== undefined) {
      writer.uint32(26).string(message.name);
    }
    if (message.hash !== undefined) {
      writer.uint32(34).string(message.hash);
    }
    if (message.reason !== undefined) {
      writer.uint32(42).string(message.reason);
    }
    if (message.start !== undefined) {
      writer.uint32(50).string(message.start);
    }
    if (message.duration !== undefined) {
      writer.uint32(56).uint32(message.duration);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BanList_BanEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBanList_BanEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mask = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.start = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.duration = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BanList_BanEntry {
    return {
      address: isSet(object.address) ? Buffer.from(bytesFromBase64(object.address)) : undefined,
      mask: isSet(object.mask) ? globalThis.Number(object.mask) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      hash: isSet(object.hash) ? globalThis.String(object.hash) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
      start: isSet(object.start) ? globalThis.String(object.start) : undefined,
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : undefined,
    };
  },

  toJSON(message: BanList_BanEntry): unknown {
    const obj: any = {};
    if (message.address !== undefined) {
      obj.address = base64FromBytes(message.address);
    }
    if (message.mask !== undefined) {
      obj.mask = Math.round(message.mask);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.hash !== undefined) {
      obj.hash = message.hash;
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    if (message.start !== undefined) {
      obj.start = message.start;
    }
    if (message.duration !== undefined) {
      obj.duration = Math.round(message.duration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BanList_BanEntry>, I>>(base?: I): BanList_BanEntry {
    return BanList_BanEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BanList_BanEntry>, I>>(object: I): BanList_BanEntry {
    const message = createBaseBanList_BanEntry();
    message.address = object.address ?? undefined;
    message.mask = object.mask ?? undefined;
    message.name = object.name ?? undefined;
    message.hash = object.hash ?? undefined;
    message.reason = object.reason ?? undefined;
    message.start = object.start ?? undefined;
    message.duration = object.duration ?? undefined;
    return message;
  },
};

function createBaseTextMessage(): TextMessage {
  return { actor: undefined, session: [], channel_id: [], tree_id: [], message: undefined };
}

export const TextMessage: MessageFns<TextMessage> = {
  encode(message: TextMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actor !== undefined) {
      writer.uint32(8).uint32(message.actor);
    }
    if (message.session !== undefined && message.session.length !== 0) {
      for (const v of message.session) {
        writer.uint32(16).uint32(v!);
      }
    }
    if (message.channel_id !== undefined && message.channel_id.length !== 0) {
      for (const v of message.channel_id) {
        writer.uint32(24).uint32(v!);
      }
    }
    if (message.tree_id !== undefined && message.tree_id.length !== 0) {
      for (const v of message.tree_id) {
        writer.uint32(32).uint32(v!);
      }
    }
    if (message.message !== undefined) {
      writer.uint32(42).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.actor = reader.uint32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.session!.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.session!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.channel_id!.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.channel_id!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.tree_id!.push(reader.uint32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.tree_id!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextMessage {
    return {
      actor: isSet(object.actor) ? globalThis.Number(object.actor) : undefined,
      session: globalThis.Array.isArray(object?.session) ? object.session.map((e: any) => globalThis.Number(e)) : [],
      channel_id: globalThis.Array.isArray(object?.channel_id)
        ? object.channel_id.map((e: any) => globalThis.Number(e))
        : [],
      tree_id: globalThis.Array.isArray(object?.tree_id) ? object.tree_id.map((e: any) => globalThis.Number(e)) : [],
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: TextMessage): unknown {
    const obj: any = {};
    if (message.actor !== undefined) {
      obj.actor = Math.round(message.actor);
    }
    if (message.session?.length) {
      obj.session = message.session.map((e) => Math.round(e));
    }
    if (message.channel_id?.length) {
      obj.channel_id = message.channel_id.map((e) => Math.round(e));
    }
    if (message.tree_id?.length) {
      obj.tree_id = message.tree_id.map((e) => Math.round(e));
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TextMessage>, I>>(base?: I): TextMessage {
    return TextMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TextMessage>, I>>(object: I): TextMessage {
    const message = createBaseTextMessage();
    message.actor = object.actor ?? undefined;
    message.session = object.session?.map((e) => e) || [];
    message.channel_id = object.channel_id?.map((e) => e) || [];
    message.tree_id = object.tree_id?.map((e) => e) || [];
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBasePermissionDenied(): PermissionDenied {
  return {
    permission: undefined,
    channel_id: undefined,
    session: undefined,
    reason: undefined,
    type: undefined,
    name: undefined,
  };
}

export const PermissionDenied: MessageFns<PermissionDenied> = {
  encode(message: PermissionDenied, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permission !== undefined) {
      writer.uint32(8).uint32(message.permission);
    }
    if (message.channel_id !== undefined) {
      writer.uint32(16).uint32(message.channel_id);
    }
    if (message.session !== undefined) {
      writer.uint32(24).uint32(message.session);
    }
    if (message.reason !== undefined) {
      writer.uint32(34).string(message.reason);
    }
    if (message.type !== undefined) {
      writer.uint32(40).int32(message.type);
    }
    if (message.name !== undefined) {
      writer.uint32(50).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionDenied {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionDenied();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.permission = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.session = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionDenied {
    return {
      permission: isSet(object.permission) ? globalThis.Number(object.permission) : undefined,
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      session: isSet(object.session) ? globalThis.Number(object.session) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
      type: isSet(object.type) ? permissionDenied_DenyTypeFromJSON(object.type) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: PermissionDenied): unknown {
    const obj: any = {};
    if (message.permission !== undefined) {
      obj.permission = Math.round(message.permission);
    }
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.session !== undefined) {
      obj.session = Math.round(message.session);
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    if (message.type !== undefined) {
      obj.type = permissionDenied_DenyTypeToJSON(message.type);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PermissionDenied>, I>>(base?: I): PermissionDenied {
    return PermissionDenied.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PermissionDenied>, I>>(object: I): PermissionDenied {
    const message = createBasePermissionDenied();
    message.permission = object.permission ?? undefined;
    message.channel_id = object.channel_id ?? undefined;
    message.session = object.session ?? undefined;
    message.reason = object.reason ?? undefined;
    message.type = object.type ?? undefined;
    message.name = object.name ?? undefined;
    return message;
  },
};

function createBaseACL(): ACL {
  return { channel_id: undefined, inherit_acls: undefined, groups: [], acls: [], query: undefined };
}

export const ACL: MessageFns<ACL> = {
  encode(message: ACL, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel_id !== undefined) {
      writer.uint32(8).uint32(message.channel_id);
    }
    if (message.inherit_acls !== undefined) {
      writer.uint32(16).bool(message.inherit_acls);
    }
    if (message.groups !== undefined && message.groups.length !== 0) {
      for (const v of message.groups) {
        ACL_ChanGroup.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.acls !== undefined && message.acls.length !== 0) {
      for (const v of message.acls) {
        ACL_ChanACL.encode(v!, writer.uint32(34).fork()).join();
      }
    }
    if (message.query !== undefined) {
      writer.uint32(40).bool(message.query);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ACL {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseACL();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.inherit_acls = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = ACL_ChanGroup.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.groups!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const el = ACL_ChanACL.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.acls!.push(el);
          }
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.query = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ACL {
    return {
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      inherit_acls: isSet(object.inherit_acls) ? globalThis.Boolean(object.inherit_acls) : undefined,
      groups: globalThis.Array.isArray(object?.groups) ? object.groups.map((e: any) => ACL_ChanGroup.fromJSON(e)) : [],
      acls: globalThis.Array.isArray(object?.acls) ? object.acls.map((e: any) => ACL_ChanACL.fromJSON(e)) : [],
      query: isSet(object.query) ? globalThis.Boolean(object.query) : undefined,
    };
  },

  toJSON(message: ACL): unknown {
    const obj: any = {};
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.inherit_acls !== undefined) {
      obj.inherit_acls = message.inherit_acls;
    }
    if (message.groups?.length) {
      obj.groups = message.groups.map((e) => ACL_ChanGroup.toJSON(e));
    }
    if (message.acls?.length) {
      obj.acls = message.acls.map((e) => ACL_ChanACL.toJSON(e));
    }
    if (message.query !== undefined) {
      obj.query = message.query;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ACL>, I>>(base?: I): ACL {
    return ACL.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ACL>, I>>(object: I): ACL {
    const message = createBaseACL();
    message.channel_id = object.channel_id ?? undefined;
    message.inherit_acls = object.inherit_acls ?? undefined;
    message.groups = object.groups?.map((e) => ACL_ChanGroup.fromPartial(e)) || [];
    message.acls = object.acls?.map((e) => ACL_ChanACL.fromPartial(e)) || [];
    message.query = object.query ?? undefined;
    return message;
  },
};

function createBaseACL_ChanGroup(): ACL_ChanGroup {
  return {
    name: undefined,
    inherited: undefined,
    inherit: undefined,
    inheritable: undefined,
    add: [],
    remove: [],
    inherited_members: [],
  };
}

export const ACL_ChanGroup: MessageFns<ACL_ChanGroup> = {
  encode(message: ACL_ChanGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined) {
      writer.uint32(10).string(message.name);
    }
    if (message.inherited !== undefined) {
      writer.uint32(16).bool(message.inherited);
    }
    if (message.inherit !== undefined) {
      writer.uint32(24).bool(message.inherit);
    }
    if (message.inheritable !== undefined) {
      writer.uint32(32).bool(message.inheritable);
    }
    if (message.add !== undefined && message.add.length !== 0) {
      for (const v of message.add) {
        writer.uint32(40).uint32(v!);
      }
    }
    if (message.remove !== undefined && message.remove.length !== 0) {
      for (const v of message.remove) {
        writer.uint32(48).uint32(v!);
      }
    }
    if (message.inherited_members !== undefined && message.inherited_members.length !== 0) {
      for (const v of message.inherited_members) {
        writer.uint32(56).uint32(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ACL_ChanGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseACL_ChanGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.inherited = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.inherit = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.inheritable = reader.bool();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.add!.push(reader.uint32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.add!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag === 48) {
            message.remove!.push(reader.uint32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.remove!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag === 56) {
            message.inherited_members!.push(reader.uint32());

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.inherited_members!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ACL_ChanGroup {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      inherited: isSet(object.inherited) ? globalThis.Boolean(object.inherited) : undefined,
      inherit: isSet(object.inherit) ? globalThis.Boolean(object.inherit) : undefined,
      inheritable: isSet(object.inheritable) ? globalThis.Boolean(object.inheritable) : undefined,
      add: globalThis.Array.isArray(object?.add) ? object.add.map((e: any) => globalThis.Number(e)) : [],
      remove: globalThis.Array.isArray(object?.remove) ? object.remove.map((e: any) => globalThis.Number(e)) : [],
      inherited_members: globalThis.Array.isArray(object?.inherited_members)
        ? object.inherited_members.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ACL_ChanGroup): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.inherited !== undefined) {
      obj.inherited = message.inherited;
    }
    if (message.inherit !== undefined) {
      obj.inherit = message.inherit;
    }
    if (message.inheritable !== undefined) {
      obj.inheritable = message.inheritable;
    }
    if (message.add?.length) {
      obj.add = message.add.map((e) => Math.round(e));
    }
    if (message.remove?.length) {
      obj.remove = message.remove.map((e) => Math.round(e));
    }
    if (message.inherited_members?.length) {
      obj.inherited_members = message.inherited_members.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ACL_ChanGroup>, I>>(base?: I): ACL_ChanGroup {
    return ACL_ChanGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ACL_ChanGroup>, I>>(object: I): ACL_ChanGroup {
    const message = createBaseACL_ChanGroup();
    message.name = object.name ?? undefined;
    message.inherited = object.inherited ?? undefined;
    message.inherit = object.inherit ?? undefined;
    message.inheritable = object.inheritable ?? undefined;
    message.add = object.add?.map((e) => e) || [];
    message.remove = object.remove?.map((e) => e) || [];
    message.inherited_members = object.inherited_members?.map((e) => e) || [];
    return message;
  },
};

function createBaseACL_ChanACL(): ACL_ChanACL {
  return {
    apply_here: undefined,
    apply_subs: undefined,
    inherited: undefined,
    user_id: undefined,
    group: undefined,
    grant: undefined,
    deny: undefined,
  };
}

export const ACL_ChanACL: MessageFns<ACL_ChanACL> = {
  encode(message: ACL_ChanACL, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apply_here !== undefined) {
      writer.uint32(8).bool(message.apply_here);
    }
    if (message.apply_subs !== undefined) {
      writer.uint32(16).bool(message.apply_subs);
    }
    if (message.inherited !== undefined) {
      writer.uint32(24).bool(message.inherited);
    }
    if (message.user_id !== undefined) {
      writer.uint32(32).uint32(message.user_id);
    }
    if (message.group !== undefined) {
      writer.uint32(42).string(message.group);
    }
    if (message.grant !== undefined) {
      writer.uint32(48).uint32(message.grant);
    }
    if (message.deny !== undefined) {
      writer.uint32(56).uint32(message.deny);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ACL_ChanACL {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseACL_ChanACL();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.apply_here = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.apply_subs = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.inherited = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.user_id = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.group = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.grant = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.deny = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ACL_ChanACL {
    return {
      apply_here: isSet(object.apply_here) ? globalThis.Boolean(object.apply_here) : undefined,
      apply_subs: isSet(object.apply_subs) ? globalThis.Boolean(object.apply_subs) : undefined,
      inherited: isSet(object.inherited) ? globalThis.Boolean(object.inherited) : undefined,
      user_id: isSet(object.user_id) ? globalThis.Number(object.user_id) : undefined,
      group: isSet(object.group) ? globalThis.String(object.group) : undefined,
      grant: isSet(object.grant) ? globalThis.Number(object.grant) : undefined,
      deny: isSet(object.deny) ? globalThis.Number(object.deny) : undefined,
    };
  },

  toJSON(message: ACL_ChanACL): unknown {
    const obj: any = {};
    if (message.apply_here !== undefined) {
      obj.apply_here = message.apply_here;
    }
    if (message.apply_subs !== undefined) {
      obj.apply_subs = message.apply_subs;
    }
    if (message.inherited !== undefined) {
      obj.inherited = message.inherited;
    }
    if (message.user_id !== undefined) {
      obj.user_id = Math.round(message.user_id);
    }
    if (message.group !== undefined) {
      obj.group = message.group;
    }
    if (message.grant !== undefined) {
      obj.grant = Math.round(message.grant);
    }
    if (message.deny !== undefined) {
      obj.deny = Math.round(message.deny);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ACL_ChanACL>, I>>(base?: I): ACL_ChanACL {
    return ACL_ChanACL.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ACL_ChanACL>, I>>(object: I): ACL_ChanACL {
    const message = createBaseACL_ChanACL();
    message.apply_here = object.apply_here ?? undefined;
    message.apply_subs = object.apply_subs ?? undefined;
    message.inherited = object.inherited ?? undefined;
    message.user_id = object.user_id ?? undefined;
    message.group = object.group ?? undefined;
    message.grant = object.grant ?? undefined;
    message.deny = object.deny ?? undefined;
    return message;
  },
};

function createBaseQueryUsers(): QueryUsers {
  return { ids: [], names: [] };
}

export const QueryUsers: MessageFns<QueryUsers> = {
  encode(message: QueryUsers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ids !== undefined && message.ids.length !== 0) {
      for (const v of message.ids) {
        writer.uint32(8).uint32(v!);
      }
    }
    if (message.names !== undefined && message.names.length !== 0) {
      for (const v of message.names) {
        writer.uint32(18).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryUsers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryUsers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.ids!.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = reader.string();
          if (el !== undefined) {
            message.names!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryUsers {
    return {
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.Number(e)) : [],
      names: globalThis.Array.isArray(object?.names) ? object.names.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: QueryUsers): unknown {
    const obj: any = {};
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Math.round(e));
    }
    if (message.names?.length) {
      obj.names = message.names;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryUsers>, I>>(base?: I): QueryUsers {
    return QueryUsers.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryUsers>, I>>(object: I): QueryUsers {
    const message = createBaseQueryUsers();
    message.ids = object.ids?.map((e) => e) || [];
    message.names = object.names?.map((e) => e) || [];
    return message;
  },
};

function createBaseCryptSetup(): CryptSetup {
  return { key: undefined, client_nonce: undefined, server_nonce: undefined };
}

export const CryptSetup: MessageFns<CryptSetup> = {
  encode(message: CryptSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined) {
      writer.uint32(10).bytes(message.key);
    }
    if (message.client_nonce !== undefined) {
      writer.uint32(18).bytes(message.client_nonce);
    }
    if (message.server_nonce !== undefined) {
      writer.uint32(26).bytes(message.server_nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CryptSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCryptSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.client_nonce = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.server_nonce = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CryptSetup {
    return {
      key: isSet(object.key) ? Buffer.from(bytesFromBase64(object.key)) : undefined,
      client_nonce: isSet(object.client_nonce) ? Buffer.from(bytesFromBase64(object.client_nonce)) : undefined,
      server_nonce: isSet(object.server_nonce) ? Buffer.from(bytesFromBase64(object.server_nonce)) : undefined,
    };
  },

  toJSON(message: CryptSetup): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.client_nonce !== undefined) {
      obj.client_nonce = base64FromBytes(message.client_nonce);
    }
    if (message.server_nonce !== undefined) {
      obj.server_nonce = base64FromBytes(message.server_nonce);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CryptSetup>, I>>(base?: I): CryptSetup {
    return CryptSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CryptSetup>, I>>(object: I): CryptSetup {
    const message = createBaseCryptSetup();
    message.key = object.key ?? undefined;
    message.client_nonce = object.client_nonce ?? undefined;
    message.server_nonce = object.server_nonce ?? undefined;
    return message;
  },
};

function createBaseContextActionModify(): ContextActionModify {
  return { action: undefined, text: undefined, context: undefined, operation: undefined };
}

export const ContextActionModify: MessageFns<ContextActionModify> = {
  encode(message: ContextActionModify, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action !== undefined) {
      writer.uint32(10).string(message.action);
    }
    if (message.text !== undefined) {
      writer.uint32(18).string(message.text);
    }
    if (message.context !== undefined) {
      writer.uint32(24).uint32(message.context);
    }
    if (message.operation !== undefined) {
      writer.uint32(32).int32(message.operation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContextActionModify {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextActionModify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.context = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.operation = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextActionModify {
    return {
      action: isSet(object.action) ? globalThis.String(object.action) : undefined,
      text: isSet(object.text) ? globalThis.String(object.text) : undefined,
      context: isSet(object.context) ? globalThis.Number(object.context) : undefined,
      operation: isSet(object.operation) ? contextActionModify_OperationFromJSON(object.operation) : undefined,
    };
  },

  toJSON(message: ContextActionModify): unknown {
    const obj: any = {};
    if (message.action !== undefined) {
      obj.action = message.action;
    }
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.context !== undefined) {
      obj.context = Math.round(message.context);
    }
    if (message.operation !== undefined) {
      obj.operation = contextActionModify_OperationToJSON(message.operation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextActionModify>, I>>(base?: I): ContextActionModify {
    return ContextActionModify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextActionModify>, I>>(object: I): ContextActionModify {
    const message = createBaseContextActionModify();
    message.action = object.action ?? undefined;
    message.text = object.text ?? undefined;
    message.context = object.context ?? undefined;
    message.operation = object.operation ?? undefined;
    return message;
  },
};

function createBaseContextAction(): ContextAction {
  return { session: undefined, channel_id: undefined, action: undefined };
}

export const ContextAction: MessageFns<ContextAction> = {
  encode(message: ContextAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      writer.uint32(8).uint32(message.session);
    }
    if (message.channel_id !== undefined) {
      writer.uint32(16).uint32(message.channel_id);
    }
    if (message.action !== undefined) {
      writer.uint32(26).string(message.action);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContextAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.session = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextAction {
    return {
      session: isSet(object.session) ? globalThis.Number(object.session) : undefined,
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      action: isSet(object.action) ? globalThis.String(object.action) : undefined,
    };
  },

  toJSON(message: ContextAction): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = Math.round(message.session);
    }
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.action !== undefined) {
      obj.action = message.action;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextAction>, I>>(base?: I): ContextAction {
    return ContextAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextAction>, I>>(object: I): ContextAction {
    const message = createBaseContextAction();
    message.session = object.session ?? undefined;
    message.channel_id = object.channel_id ?? undefined;
    message.action = object.action ?? undefined;
    return message;
  },
};

function createBaseUserList(): UserList {
  return { users: [] };
}

export const UserList: MessageFns<UserList> = {
  encode(message: UserList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.users !== undefined && message.users.length !== 0) {
      for (const v of message.users) {
        UserList_User.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const el = UserList_User.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.users!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserList {
    return {
      users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => UserList_User.fromJSON(e)) : [],
    };
  },

  toJSON(message: UserList): unknown {
    const obj: any = {};
    if (message.users?.length) {
      obj.users = message.users.map((e) => UserList_User.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserList>, I>>(base?: I): UserList {
    return UserList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserList>, I>>(object: I): UserList {
    const message = createBaseUserList();
    message.users = object.users?.map((e) => UserList_User.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUserList_User(): UserList_User {
  return { user_id: undefined, name: undefined, last_seen: undefined, last_channel: undefined };
}

export const UserList_User: MessageFns<UserList_User> = {
  encode(message: UserList_User, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user_id !== undefined) {
      writer.uint32(8).uint32(message.user_id);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.last_seen !== undefined) {
      writer.uint32(26).string(message.last_seen);
    }
    if (message.last_channel !== undefined) {
      writer.uint32(32).uint32(message.last_channel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserList_User {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserList_User();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.user_id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.last_seen = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.last_channel = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserList_User {
    return {
      user_id: isSet(object.user_id) ? globalThis.Number(object.user_id) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      last_seen: isSet(object.last_seen) ? globalThis.String(object.last_seen) : undefined,
      last_channel: isSet(object.last_channel) ? globalThis.Number(object.last_channel) : undefined,
    };
  },

  toJSON(message: UserList_User): unknown {
    const obj: any = {};
    if (message.user_id !== undefined) {
      obj.user_id = Math.round(message.user_id);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.last_seen !== undefined) {
      obj.last_seen = message.last_seen;
    }
    if (message.last_channel !== undefined) {
      obj.last_channel = Math.round(message.last_channel);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserList_User>, I>>(base?: I): UserList_User {
    return UserList_User.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserList_User>, I>>(object: I): UserList_User {
    const message = createBaseUserList_User();
    message.user_id = object.user_id ?? undefined;
    message.name = object.name ?? undefined;
    message.last_seen = object.last_seen ?? undefined;
    message.last_channel = object.last_channel ?? undefined;
    return message;
  },
};

function createBaseVoiceTarget(): VoiceTarget {
  return { id: undefined, targets: [] };
}

export const VoiceTarget: MessageFns<VoiceTarget> = {
  encode(message: VoiceTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.targets !== undefined && message.targets.length !== 0) {
      for (const v of message.targets) {
        VoiceTarget_Target.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoiceTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoiceTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const el = VoiceTarget_Target.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.targets!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoiceTarget {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : undefined,
      targets: globalThis.Array.isArray(object?.targets)
        ? object.targets.map((e: any) => VoiceTarget_Target.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VoiceTarget): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Math.round(message.id);
    }
    if (message.targets?.length) {
      obj.targets = message.targets.map((e) => VoiceTarget_Target.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoiceTarget>, I>>(base?: I): VoiceTarget {
    return VoiceTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoiceTarget>, I>>(object: I): VoiceTarget {
    const message = createBaseVoiceTarget();
    message.id = object.id ?? undefined;
    message.targets = object.targets?.map((e) => VoiceTarget_Target.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVoiceTarget_Target(): VoiceTarget_Target {
  return { session: [], channel_id: undefined, group: undefined, links: undefined, children: undefined };
}

export const VoiceTarget_Target: MessageFns<VoiceTarget_Target> = {
  encode(message: VoiceTarget_Target, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined && message.session.length !== 0) {
      for (const v of message.session) {
        writer.uint32(8).uint32(v!);
      }
    }
    if (message.channel_id !== undefined) {
      writer.uint32(16).uint32(message.channel_id);
    }
    if (message.group !== undefined) {
      writer.uint32(26).string(message.group);
    }
    if (message.links !== undefined) {
      writer.uint32(32).bool(message.links);
    }
    if (message.children !== undefined) {
      writer.uint32(40).bool(message.children);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoiceTarget_Target {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoiceTarget_Target();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.session!.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.session!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.group = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.links = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.children = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoiceTarget_Target {
    return {
      session: globalThis.Array.isArray(object?.session) ? object.session.map((e: any) => globalThis.Number(e)) : [],
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      group: isSet(object.group) ? globalThis.String(object.group) : undefined,
      links: isSet(object.links) ? globalThis.Boolean(object.links) : undefined,
      children: isSet(object.children) ? globalThis.Boolean(object.children) : undefined,
    };
  },

  toJSON(message: VoiceTarget_Target): unknown {
    const obj: any = {};
    if (message.session?.length) {
      obj.session = message.session.map((e) => Math.round(e));
    }
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.group !== undefined) {
      obj.group = message.group;
    }
    if (message.links !== undefined) {
      obj.links = message.links;
    }
    if (message.children !== undefined) {
      obj.children = message.children;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoiceTarget_Target>, I>>(base?: I): VoiceTarget_Target {
    return VoiceTarget_Target.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoiceTarget_Target>, I>>(object: I): VoiceTarget_Target {
    const message = createBaseVoiceTarget_Target();
    message.session = object.session?.map((e) => e) || [];
    message.channel_id = object.channel_id ?? undefined;
    message.group = object.group ?? undefined;
    message.links = object.links ?? undefined;
    message.children = object.children ?? undefined;
    return message;
  },
};

function createBasePermissionQuery(): PermissionQuery {
  return { channel_id: undefined, permissions: undefined, flush: undefined };
}

export const PermissionQuery: MessageFns<PermissionQuery> = {
  encode(message: PermissionQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel_id !== undefined) {
      writer.uint32(8).uint32(message.channel_id);
    }
    if (message.permissions !== undefined) {
      writer.uint32(16).uint32(message.permissions);
    }
    if (message.flush !== undefined) {
      writer.uint32(24).bool(message.flush);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel_id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.permissions = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.flush = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionQuery {
    return {
      channel_id: isSet(object.channel_id) ? globalThis.Number(object.channel_id) : undefined,
      permissions: isSet(object.permissions) ? globalThis.Number(object.permissions) : undefined,
      flush: isSet(object.flush) ? globalThis.Boolean(object.flush) : undefined,
    };
  },

  toJSON(message: PermissionQuery): unknown {
    const obj: any = {};
    if (message.channel_id !== undefined) {
      obj.channel_id = Math.round(message.channel_id);
    }
    if (message.permissions !== undefined) {
      obj.permissions = Math.round(message.permissions);
    }
    if (message.flush !== undefined) {
      obj.flush = message.flush;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PermissionQuery>, I>>(base?: I): PermissionQuery {
    return PermissionQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PermissionQuery>, I>>(object: I): PermissionQuery {
    const message = createBasePermissionQuery();
    message.channel_id = object.channel_id ?? undefined;
    message.permissions = object.permissions ?? undefined;
    message.flush = object.flush ?? undefined;
    return message;
  },
};

function createBaseCodecVersion(): CodecVersion {
  return { alpha: undefined, beta: undefined, prefer_alpha: undefined, opus: undefined };
}

export const CodecVersion: MessageFns<CodecVersion> = {
  encode(message: CodecVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alpha !== undefined) {
      writer.uint32(8).int32(message.alpha);
    }
    if (message.beta !== undefined) {
      writer.uint32(16).int32(message.beta);
    }
    if (message.prefer_alpha !== undefined) {
      writer.uint32(24).bool(message.prefer_alpha);
    }
    if (message.opus !== undefined) {
      writer.uint32(32).bool(message.opus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodecVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodecVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.alpha = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.beta = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.prefer_alpha = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.opus = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CodecVersion {
    return {
      alpha: isSet(object.alpha) ? globalThis.Number(object.alpha) : undefined,
      beta: isSet(object.beta) ? globalThis.Number(object.beta) : undefined,
      prefer_alpha: isSet(object.prefer_alpha) ? globalThis.Boolean(object.prefer_alpha) : undefined,
      opus: isSet(object.opus) ? globalThis.Boolean(object.opus) : undefined,
    };
  },

  toJSON(message: CodecVersion): unknown {
    const obj: any = {};
    if (message.alpha !== undefined) {
      obj.alpha = Math.round(message.alpha);
    }
    if (message.beta !== undefined) {
      obj.beta = Math.round(message.beta);
    }
    if (message.prefer_alpha !== undefined) {
      obj.prefer_alpha = message.prefer_alpha;
    }
    if (message.opus !== undefined) {
      obj.opus = message.opus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CodecVersion>, I>>(base?: I): CodecVersion {
    return CodecVersion.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodecVersion>, I>>(object: I): CodecVersion {
    const message = createBaseCodecVersion();
    message.alpha = object.alpha ?? undefined;
    message.beta = object.beta ?? undefined;
    message.prefer_alpha = object.prefer_alpha ?? undefined;
    message.opus = object.opus ?? undefined;
    return message;
  },
};

function createBaseUserStats(): UserStats {
  return {
    session: undefined,
    stats_only: undefined,
    certificates: [],
    from_client: undefined,
    from_server: undefined,
    udp_packets: undefined,
    tcp_packets: undefined,
    udp_ping_avg: undefined,
    udp_ping_var: undefined,
    tcp_ping_avg: undefined,
    tcp_ping_var: undefined,
    version: undefined,
    celt_versions: [],
    address: undefined,
    bandwidth: undefined,
    onlinesecs: undefined,
    idlesecs: undefined,
    strong_certificate: undefined,
    opus: undefined,
  };
}

export const UserStats: MessageFns<UserStats> = {
  encode(message: UserStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      writer.uint32(8).uint32(message.session);
    }
    if (message.stats_only !== undefined) {
      writer.uint32(16).bool(message.stats_only);
    }
    if (message.certificates !== undefined && message.certificates.length !== 0) {
      for (const v of message.certificates) {
        writer.uint32(26).bytes(v!);
      }
    }
    if (message.from_client !== undefined) {
      UserStats_Stats.encode(message.from_client, writer.uint32(34).fork()).join();
    }
    if (message.from_server !== undefined) {
      UserStats_Stats.encode(message.from_server, writer.uint32(42).fork()).join();
    }
    if (message.udp_packets !== undefined) {
      writer.uint32(48).uint32(message.udp_packets);
    }
    if (message.tcp_packets !== undefined) {
      writer.uint32(56).uint32(message.tcp_packets);
    }
    if (message.udp_ping_avg !== undefined) {
      writer.uint32(69).float(message.udp_ping_avg);
    }
    if (message.udp_ping_var !== undefined) {
      writer.uint32(77).float(message.udp_ping_var);
    }
    if (message.tcp_ping_avg !== undefined) {
      writer.uint32(85).float(message.tcp_ping_avg);
    }
    if (message.tcp_ping_var !== undefined) {
      writer.uint32(93).float(message.tcp_ping_var);
    }
    if (message.version !== undefined) {
      Version.encode(message.version, writer.uint32(98).fork()).join();
    }
    if (message.celt_versions !== undefined && message.celt_versions.length !== 0) {
      for (const v of message.celt_versions) {
        writer.uint32(104).int32(v!);
      }
    }
    if (message.address !== undefined) {
      writer.uint32(114).bytes(message.address);
    }
    if (message.bandwidth !== undefined) {
      writer.uint32(120).uint32(message.bandwidth);
    }
    if (message.onlinesecs !== undefined) {
      writer.uint32(128).uint32(message.onlinesecs);
    }
    if (message.idlesecs !== undefined) {
      writer.uint32(136).uint32(message.idlesecs);
    }
    if (message.strong_certificate !== undefined) {
      writer.uint32(144).bool(message.strong_certificate);
    }
    if (message.opus !== undefined) {
      writer.uint32(152).bool(message.opus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.session = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stats_only = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const el = Buffer.from(reader.bytes());
          if (el !== undefined) {
            message.certificates!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.from_client = UserStats_Stats.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.from_server = UserStats_Stats.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.udp_packets = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tcp_packets = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.udp_ping_avg = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.udp_ping_var = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 85) {
            break;
          }

          message.tcp_ping_avg = reader.float();
          continue;
        }
        case 11: {
          if (tag !== 93) {
            break;
          }

          message.tcp_ping_var = reader.float();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.version = Version.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag === 104) {
            message.celt_versions!.push(reader.int32());

            continue;
          }

          if (tag === 106) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.celt_versions!.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.address = Buffer.from(reader.bytes());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.bandwidth = reader.uint32();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.onlinesecs = reader.uint32();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.idlesecs = reader.uint32();
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.strong_certificate = reader.bool();
          continue;
        }
        case 19: {
          if (tag !== 152) {
            break;
          }

          message.opus = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserStats {
    return {
      session: isSet(object.session) ? globalThis.Number(object.session) : undefined,
      stats_only: isSet(object.stats_only) ? globalThis.Boolean(object.stats_only) : undefined,
      certificates: globalThis.Array.isArray(object?.certificates)
        ? object.certificates.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
      from_client: isSet(object.from_client) ? UserStats_Stats.fromJSON(object.from_client) : undefined,
      from_server: isSet(object.from_server) ? UserStats_Stats.fromJSON(object.from_server) : undefined,
      udp_packets: isSet(object.udp_packets) ? globalThis.Number(object.udp_packets) : undefined,
      tcp_packets: isSet(object.tcp_packets) ? globalThis.Number(object.tcp_packets) : undefined,
      udp_ping_avg: isSet(object.udp_ping_avg) ? globalThis.Number(object.udp_ping_avg) : undefined,
      udp_ping_var: isSet(object.udp_ping_var) ? globalThis.Number(object.udp_ping_var) : undefined,
      tcp_ping_avg: isSet(object.tcp_ping_avg) ? globalThis.Number(object.tcp_ping_avg) : undefined,
      tcp_ping_var: isSet(object.tcp_ping_var) ? globalThis.Number(object.tcp_ping_var) : undefined,
      version: isSet(object.version) ? Version.fromJSON(object.version) : undefined,
      celt_versions: globalThis.Array.isArray(object?.celt_versions)
        ? object.celt_versions.map((e: any) => globalThis.Number(e))
        : [],
      address: isSet(object.address) ? Buffer.from(bytesFromBase64(object.address)) : undefined,
      bandwidth: isSet(object.bandwidth) ? globalThis.Number(object.bandwidth) : undefined,
      onlinesecs: isSet(object.onlinesecs) ? globalThis.Number(object.onlinesecs) : undefined,
      idlesecs: isSet(object.idlesecs) ? globalThis.Number(object.idlesecs) : undefined,
      strong_certificate: isSet(object.strong_certificate) ? globalThis.Boolean(object.strong_certificate) : undefined,
      opus: isSet(object.opus) ? globalThis.Boolean(object.opus) : undefined,
    };
  },

  toJSON(message: UserStats): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = Math.round(message.session);
    }
    if (message.stats_only !== undefined) {
      obj.stats_only = message.stats_only;
    }
    if (message.certificates?.length) {
      obj.certificates = message.certificates.map((e) => base64FromBytes(e));
    }
    if (message.from_client !== undefined) {
      obj.from_client = UserStats_Stats.toJSON(message.from_client);
    }
    if (message.from_server !== undefined) {
      obj.from_server = UserStats_Stats.toJSON(message.from_server);
    }
    if (message.udp_packets !== undefined) {
      obj.udp_packets = Math.round(message.udp_packets);
    }
    if (message.tcp_packets !== undefined) {
      obj.tcp_packets = Math.round(message.tcp_packets);
    }
    if (message.udp_ping_avg !== undefined) {
      obj.udp_ping_avg = message.udp_ping_avg;
    }
    if (message.udp_ping_var !== undefined) {
      obj.udp_ping_var = message.udp_ping_var;
    }
    if (message.tcp_ping_avg !== undefined) {
      obj.tcp_ping_avg = message.tcp_ping_avg;
    }
    if (message.tcp_ping_var !== undefined) {
      obj.tcp_ping_var = message.tcp_ping_var;
    }
    if (message.version !== undefined) {
      obj.version = Version.toJSON(message.version);
    }
    if (message.celt_versions?.length) {
      obj.celt_versions = message.celt_versions.map((e) => Math.round(e));
    }
    if (message.address !== undefined) {
      obj.address = base64FromBytes(message.address);
    }
    if (message.bandwidth !== undefined) {
      obj.bandwidth = Math.round(message.bandwidth);
    }
    if (message.onlinesecs !== undefined) {
      obj.onlinesecs = Math.round(message.onlinesecs);
    }
    if (message.idlesecs !== undefined) {
      obj.idlesecs = Math.round(message.idlesecs);
    }
    if (message.strong_certificate !== undefined) {
      obj.strong_certificate = message.strong_certificate;
    }
    if (message.opus !== undefined) {
      obj.opus = message.opus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserStats>, I>>(base?: I): UserStats {
    return UserStats.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserStats>, I>>(object: I): UserStats {
    const message = createBaseUserStats();
    message.session = object.session ?? undefined;
    message.stats_only = object.stats_only ?? undefined;
    message.certificates = object.certificates?.map((e) => e) || [];
    message.from_client = (object.from_client !== undefined && object.from_client !== null)
      ? UserStats_Stats.fromPartial(object.from_client)
      : undefined;
    message.from_server = (object.from_server !== undefined && object.from_server !== null)
      ? UserStats_Stats.fromPartial(object.from_server)
      : undefined;
    message.udp_packets = object.udp_packets ?? undefined;
    message.tcp_packets = object.tcp_packets ?? undefined;
    message.udp_ping_avg = object.udp_ping_avg ?? undefined;
    message.udp_ping_var = object.udp_ping_var ?? undefined;
    message.tcp_ping_avg = object.tcp_ping_avg ?? undefined;
    message.tcp_ping_var = object.tcp_ping_var ?? undefined;
    message.version = (object.version !== undefined && object.version !== null)
      ? Version.fromPartial(object.version)
      : undefined;
    message.celt_versions = object.celt_versions?.map((e) => e) || [];
    message.address = object.address ?? undefined;
    message.bandwidth = object.bandwidth ?? undefined;
    message.onlinesecs = object.onlinesecs ?? undefined;
    message.idlesecs = object.idlesecs ?? undefined;
    message.strong_certificate = object.strong_certificate ?? undefined;
    message.opus = object.opus ?? undefined;
    return message;
  },
};

function createBaseUserStats_Stats(): UserStats_Stats {
  return { good: undefined, late: undefined, lost: undefined, resync: undefined };
}

export const UserStats_Stats: MessageFns<UserStats_Stats> = {
  encode(message: UserStats_Stats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.good !== undefined) {
      writer.uint32(8).uint32(message.good);
    }
    if (message.late !== undefined) {
      writer.uint32(16).uint32(message.late);
    }
    if (message.lost !== undefined) {
      writer.uint32(24).uint32(message.lost);
    }
    if (message.resync !== undefined) {
      writer.uint32(32).uint32(message.resync);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserStats_Stats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserStats_Stats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.good = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.late = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.lost = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.resync = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserStats_Stats {
    return {
      good: isSet(object.good) ? globalThis.Number(object.good) : undefined,
      late: isSet(object.late) ? globalThis.Number(object.late) : undefined,
      lost: isSet(object.lost) ? globalThis.Number(object.lost) : undefined,
      resync: isSet(object.resync) ? globalThis.Number(object.resync) : undefined,
    };
  },

  toJSON(message: UserStats_Stats): unknown {
    const obj: any = {};
    if (message.good !== undefined) {
      obj.good = Math.round(message.good);
    }
    if (message.late !== undefined) {
      obj.late = Math.round(message.late);
    }
    if (message.lost !== undefined) {
      obj.lost = Math.round(message.lost);
    }
    if (message.resync !== undefined) {
      obj.resync = Math.round(message.resync);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserStats_Stats>, I>>(base?: I): UserStats_Stats {
    return UserStats_Stats.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserStats_Stats>, I>>(object: I): UserStats_Stats {
    const message = createBaseUserStats_Stats();
    message.good = object.good ?? undefined;
    message.late = object.late ?? undefined;
    message.lost = object.lost ?? undefined;
    message.resync = object.resync ?? undefined;
    return message;
  },
};

function createBaseRequestBlob(): RequestBlob {
  return { session_texture: [], session_comment: [], channel_description: [] };
}

export const RequestBlob: MessageFns<RequestBlob> = {
  encode(message: RequestBlob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session_texture !== undefined && message.session_texture.length !== 0) {
      for (const v of message.session_texture) {
        writer.uint32(8).uint32(v!);
      }
    }
    if (message.session_comment !== undefined && message.session_comment.length !== 0) {
      for (const v of message.session_comment) {
        writer.uint32(16).uint32(v!);
      }
    }
    if (message.channel_description !== undefined && message.channel_description.length !== 0) {
      for (const v of message.channel_description) {
        writer.uint32(24).uint32(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestBlob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestBlob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.session_texture!.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.session_texture!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.session_comment!.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.session_comment!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.channel_description!.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.channel_description!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestBlob {
    return {
      session_texture: globalThis.Array.isArray(object?.session_texture)
        ? object.session_texture.map((e: any) => globalThis.Number(e))
        : [],
      session_comment: globalThis.Array.isArray(object?.session_comment)
        ? object.session_comment.map((e: any) => globalThis.Number(e))
        : [],
      channel_description: globalThis.Array.isArray(object?.channel_description)
        ? object.channel_description.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: RequestBlob): unknown {
    const obj: any = {};
    if (message.session_texture?.length) {
      obj.session_texture = message.session_texture.map((e) => Math.round(e));
    }
    if (message.session_comment?.length) {
      obj.session_comment = message.session_comment.map((e) => Math.round(e));
    }
    if (message.channel_description?.length) {
      obj.channel_description = message.channel_description.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestBlob>, I>>(base?: I): RequestBlob {
    return RequestBlob.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestBlob>, I>>(object: I): RequestBlob {
    const message = createBaseRequestBlob();
    message.session_texture = object.session_texture?.map((e) => e) || [];
    message.session_comment = object.session_comment?.map((e) => e) || [];
    message.channel_description = object.channel_description?.map((e) => e) || [];
    return message;
  },
};

function createBaseServerConfig(): ServerConfig {
  return {
    max_bandwidth: undefined,
    welcome_text: undefined,
    allow_html: undefined,
    message_length: undefined,
    image_message_length: undefined,
    max_users: undefined,
    recording_allowed: undefined,
  };
}

export const ServerConfig: MessageFns<ServerConfig> = {
  encode(message: ServerConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max_bandwidth !== undefined) {
      writer.uint32(8).uint32(message.max_bandwidth);
    }
    if (message.welcome_text !== undefined) {
      writer.uint32(18).string(message.welcome_text);
    }
    if (message.allow_html !== undefined) {
      writer.uint32(24).bool(message.allow_html);
    }
    if (message.message_length !== undefined) {
      writer.uint32(32).uint32(message.message_length);
    }
    if (message.image_message_length !== undefined) {
      writer.uint32(40).uint32(message.image_message_length);
    }
    if (message.max_users !== undefined) {
      writer.uint32(48).uint32(message.max_users);
    }
    if (message.recording_allowed !== undefined) {
      writer.uint32(56).bool(message.recording_allowed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.max_bandwidth = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.welcome_text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.allow_html = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.message_length = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.image_message_length = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.max_users = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.recording_allowed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerConfig {
    return {
      max_bandwidth: isSet(object.max_bandwidth) ? globalThis.Number(object.max_bandwidth) : undefined,
      welcome_text: isSet(object.welcome_text) ? globalThis.String(object.welcome_text) : undefined,
      allow_html: isSet(object.allow_html) ? globalThis.Boolean(object.allow_html) : undefined,
      message_length: isSet(object.message_length) ? globalThis.Number(object.message_length) : undefined,
      image_message_length: isSet(object.image_message_length)
        ? globalThis.Number(object.image_message_length)
        : undefined,
      max_users: isSet(object.max_users) ? globalThis.Number(object.max_users) : undefined,
      recording_allowed: isSet(object.recording_allowed) ? globalThis.Boolean(object.recording_allowed) : undefined,
    };
  },

  toJSON(message: ServerConfig): unknown {
    const obj: any = {};
    if (message.max_bandwidth !== undefined) {
      obj.max_bandwidth = Math.round(message.max_bandwidth);
    }
    if (message.welcome_text !== undefined) {
      obj.welcome_text = message.welcome_text;
    }
    if (message.allow_html !== undefined) {
      obj.allow_html = message.allow_html;
    }
    if (message.message_length !== undefined) {
      obj.message_length = Math.round(message.message_length);
    }
    if (message.image_message_length !== undefined) {
      obj.image_message_length = Math.round(message.image_message_length);
    }
    if (message.max_users !== undefined) {
      obj.max_users = Math.round(message.max_users);
    }
    if (message.recording_allowed !== undefined) {
      obj.recording_allowed = message.recording_allowed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerConfig>, I>>(base?: I): ServerConfig {
    return ServerConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerConfig>, I>>(object: I): ServerConfig {
    const message = createBaseServerConfig();
    message.max_bandwidth = object.max_bandwidth ?? undefined;
    message.welcome_text = object.welcome_text ?? undefined;
    message.allow_html = object.allow_html ?? undefined;
    message.message_length = object.message_length ?? undefined;
    message.image_message_length = object.image_message_length ?? undefined;
    message.max_users = object.max_users ?? undefined;
    message.recording_allowed = object.recording_allowed ?? undefined;
    return message;
  },
};

function createBaseSuggestConfig(): SuggestConfig {
  return { version: undefined, positional: undefined, push_to_talk: undefined };
}

export const SuggestConfig: MessageFns<SuggestConfig> = {
  encode(message: SuggestConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== undefined) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.positional !== undefined) {
      writer.uint32(16).bool(message.positional);
    }
    if (message.push_to_talk !== undefined) {
      writer.uint32(24).bool(message.push_to_talk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SuggestConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSuggestConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.positional = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.push_to_talk = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SuggestConfig {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : undefined,
      positional: isSet(object.positional) ? globalThis.Boolean(object.positional) : undefined,
      push_to_talk: isSet(object.push_to_talk) ? globalThis.Boolean(object.push_to_talk) : undefined,
    };
  },

  toJSON(message: SuggestConfig): unknown {
    const obj: any = {};
    if (message.version !== undefined) {
      obj.version = Math.round(message.version);
    }
    if (message.positional !== undefined) {
      obj.positional = message.positional;
    }
    if (message.push_to_talk !== undefined) {
      obj.push_to_talk = message.push_to_talk;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SuggestConfig>, I>>(base?: I): SuggestConfig {
    return SuggestConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SuggestConfig>, I>>(object: I): SuggestConfig {
    const message = createBaseSuggestConfig();
    message.version = object.version ?? undefined;
    message.positional = object.positional ?? undefined;
    message.push_to_talk = object.push_to_talk ?? undefined;
    return message;
  },
};

function createBasePluginDataTransmission(): PluginDataTransmission {
  return { senderSession: undefined, receiverSessions: [], data: undefined, dataID: undefined };
}

export const PluginDataTransmission: MessageFns<PluginDataTransmission> = {
  encode(message: PluginDataTransmission, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.senderSession !== undefined) {
      writer.uint32(8).uint32(message.senderSession);
    }
    if (message.receiverSessions !== undefined && message.receiverSessions.length !== 0) {
      writer.uint32(18).fork();
      for (const v of message.receiverSessions) {
        writer.uint32(v);
      }
      writer.join();
    }
    if (message.data !== undefined) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.dataID !== undefined) {
      writer.uint32(34).string(message.dataID);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PluginDataTransmission {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePluginDataTransmission();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.senderSession = reader.uint32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.receiverSessions!.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.receiverSessions!.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dataID = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PluginDataTransmission {
    return {
      senderSession: isSet(object.senderSession) ? globalThis.Number(object.senderSession) : undefined,
      receiverSessions: globalThis.Array.isArray(object?.receiverSessions)
        ? object.receiverSessions.map((e: any) => globalThis.Number(e))
        : [],
      data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : undefined,
      dataID: isSet(object.dataID) ? globalThis.String(object.dataID) : undefined,
    };
  },

  toJSON(message: PluginDataTransmission): unknown {
    const obj: any = {};
    if (message.senderSession !== undefined) {
      obj.senderSession = Math.round(message.senderSession);
    }
    if (message.receiverSessions?.length) {
      obj.receiverSessions = message.receiverSessions.map((e) => Math.round(e));
    }
    if (message.data !== undefined) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.dataID !== undefined) {
      obj.dataID = message.dataID;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PluginDataTransmission>, I>>(base?: I): PluginDataTransmission {
    return PluginDataTransmission.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PluginDataTransmission>, I>>(object: I): PluginDataTransmission {
    const message = createBasePluginDataTransmission();
    message.senderSession = object.senderSession ?? undefined;
    message.receiverSessions = object.receiverSessions?.map((e) => e) || [];
    message.data = object.data ?? undefined;
    message.dataID = object.dataID ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
