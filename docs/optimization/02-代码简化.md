# MuNode 代码简化方案

## 1. 状态管理整合

### 1.1 当前问题

Edge Server 中存在多个状态管理组件，职责有重叠：

| 组件 | 文件 | 职责 |
|------|------|------|
| `EdgeStateManager` | state/state-manager.ts | 管理频道、ACL、封禁、远程用户追踪 |
| `ChannelManager` | models/channel.ts | 管理频道树结构、链接 |
| `ClientManager` | (通过factory访问) | 管理本地客户端、索引 |

**重叠点**：
- `EdgeStateManager.channels` 和 `ChannelManager.channels` 都存储频道数据
- 频道链接在两处都有维护
- 更新时需要同步两处

### 1.2 建议简化

**方案 A：EdgeStateManager 完全委托给 ChannelManager**

这是当前代码正在演进的方向（通过 `this.channelManager` 引用）。

```typescript
// EdgeStateManager 简化
export class EdgeStateManager {
  // 移除：private channels: Map<number, ChannelData> = new Map();
  // 移除：private channelTree: ChannelNode | null = null;
  // 移除：private channelLinks: Map<number, Set<number>> = new Map();
  
  private channelManager: ChannelManager; // 必须依赖
  private acls: Map<number, ACLData[]> = new Map();
  private bans: BanCache;
  private remoteUsers: ...;
  
  // 所有频道操作委托
  getChannel(channelId: number): ChannelData | undefined {
    const info = this.channelManager.getChannel(channelId);
    return info ? this.toChannelData(info) : undefined;
  }
  
  getAllChannels(): ChannelData[] {
    return this.channelManager.getAllChannels().map(this.toChannelData);
  }
}
```

**方案 B：合并为单一 StateManager**

```typescript
// 统一的状态管理器
export class UnifiedStateManager {
  // 频道管理
  private channels: Map<number, ChannelInfo> = new Map();
  private channelLinks: Map<number, Set<number>> = new Map();
  
  // 客户端管理（本地）
  private clients: Map<number, ClientInfo> = new Map();
  private clientIndexes: ClientIndexes;
  
  // 远程用户追踪
  private remoteUsers: RemoteUserTracker;
  
  // ACL 和封禁
  private acls: Map<number, ACLData[]> = new Map();
  private bans: BanCache;
  
  // 统一的事件发射
  // 便于其他组件订阅状态变化
}
```

**推荐**：方案 A，因为：
1. 改动较小
2. 保持职责分离（频道 vs 其他状态）
3. 当前代码已经在向这个方向演进

### 1.3 移除冗余代码

清理 `EdgeStateManager` 中已注释的代码：

```typescript
// 可移除（已注释，不再使用）
// private rebuildChannelTree(): void {
//   // 此方法已不再使用
// }
```

## 2. 消息处理器整合

### 2.1 当前结构

Edge Server 有多个消息处理器类：

```
packages/edge-server/src/handlers/
├── auth-handlers.ts      # 认证相关
├── channel-handlers.ts   # 频道操作
├── message-handlers.ts   # 文本消息、频道列表
├── state-handlers.ts     # 用户/频道状态
└── voice-handlers.ts     # 语音相关
```

这种分离是合理的，但存在一些问题：

1. **依赖获取繁琐**：每个 Handler 都需要通过 `HandlerFactory` 获取依赖
2. **跨 Handler 调用**：有时需要调用其他 Handler 的方法
3. **错误处理重复**：每个 Handler 都有类似的 try-catch 模式

### 2.2 建议改进

**2.2.1 工厂类初始化单例**

当前通过 `this.factory.xxx` 访问依赖，建议改为类初始化时从工厂获取：

```typescript
// 改进后的 Handler 基类
abstract class BaseHandler {
  protected readonly clientManager: ClientManager;
  protected readonly channelManager: ChannelManager;
  protected readonly hubClient: HubClient;
  protected readonly config: EdgeConfig;
  protected readonly logger: Logger;
  
  constructor(factory: HandlerFactory) {
    // 初始化时获取单例，而非每次访问时通过 getter
    this.clientManager = factory.clientManager;
    this.channelManager = factory.channelManager;
    this.hubClient = factory.hubClient;
    this.config = factory.config;
    this.logger = factory.logger;
  }
  
  // 通用方法
  protected sendPermissionDenied(sessionId: number, ...) { ... }
  protected sendReject(sessionId: number, ...) { ... }
}

// 具体 Handler 继承
class AuthHandlers extends BaseHandler {
  handleAuthenticate(session_id: number, data: Buffer) {
    // 直接使用 this.clientManager, this.hubClient 等
  }
}
```

**优点**：
- 避免每次访问都通过 getter
- 代码更简洁
- 类型更明确

**2.2.2 统一错误处理装饰器**

```typescript
// 创建统一的错误处理包装器
function handleErrors(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  const originalMethod = descriptor.value;
  
  descriptor.value = async function(...args: any[]) {
    try {
      return await originalMethod.apply(this, args);
    } catch (error) {
      const sessionId = args[0]; // 约定第一个参数是 session_id
      this.logger.error(`Error in ${propertyKey} for session ${sessionId}:`, error);
      // 可选：发送错误消息给客户端
    }
  };
  
  return descriptor;
}

// 使用示例
class AuthHandlers extends BaseHandler {
  @handleErrors
  async handleAuthenticate(session_id: number, data: Buffer) {
    // 具体逻辑，无需 try-catch
  }
}
```

## 3. RPC 类型系统简化

### 3.1 当前问题

`protocol/src/rpc/rpc-types.ts` 定义了大量的方法接口，类型推导复杂：

```typescript
// 当前：每个方法一个接口
export interface EdgeRegisterMethod { method: 'edge.register'; params: {...}; result: {...}; }
export interface EdgeHeartbeatMethod { method: 'edge.heartbeat'; params: {...}; result: {...}; }
// ... 30+ 个类似接口

// 然后组合
export type EdgeToHubMethods = EdgeRegisterMethod | EdgeHeartbeatMethod | ...;

// 再提取
export type RPCMethodMap = { [K in EdgeToHubMethods as K['method']]: K; };
export type RPCParams<M extends EdgeToHubMethods['method']> = RPCMethodMap[M]['params'];
```

### 3.2 建议简化

**方案：使用单一定义对象**

以下方案保持与当前相同的 IDE 智能提示能力：

```typescript
// 统一的方法定义
export const rpcMethodDefs = {
  'edge.register': {
    params: null as unknown as {
      server_id: number;
      name: string;
      host: string;
      port: number;
      region?: string;
      capacity: number;
      certificate: string;
      metadata?: Record<string, unknown>;
      challenge?: string;
      challenge_response?: string;
    },
    result: null as unknown as RegisterResponse,
  },
  
  'edge.heartbeat': {
    params: null as unknown as {
      server_id: number;
      stats: ServerStats;
    },
    result: null as unknown as HeartbeatResponse,
  },
  
  // ... 其他方法
} as const;

// 类型提取（与当前能力完全一致）
export type RPCMethods = typeof rpcMethodDefs;
export type RPCMethodName = keyof RPCMethods;
export type RPCParams<M extends RPCMethodName> = RPCMethods[M]['params'];
export type RPCResult<M extends RPCMethodName> = RPCMethods[M]['result'];

// 使用时 IDE 提示完全相同
async function call<M extends RPCMethodName>(
  method: M, 
  params: RPCParams<M>
): Promise<RPCResult<M>> {
  // ...
}

// 调用时有完整的类型提示
call('edge.register', {
  server_id: 1,        // ✓ 类型检查
  name: 'edge-1',      // ✓ 自动补全
  // ...
});
```

**IDE 提示对比**：
| 功能 | 当前方案 | 简化方案 |
|------|---------|---------|
| 方法名自动补全 | ✓ | ✓ |
| 参数类型检查 | ✓ | ✓ |
| 返回值类型推导 | ✓ | ✓ |
| 跳转到定义 | 跳转到接口 | 跳转到对象属性 |
| 修改时重构 | 需改接口 | 直接改对象 |

**优点**：
- 代码量减少约 50%
- 添加新方法只需在一处添加
- IDE 智能提示能力不变
- 更容易维护

### 3.3 Legacy RPC 清理

当前存在两套 RPC 系统：
- 新的 Protobuf-based（推荐）
- 旧的 TypedRPC（标记为 Legacy）

建议在确认新系统稳定后，逐步移除旧系统：

```typescript
// protocol/src/index.ts 中的注释
// Typed RPC (Legacy - will be deprecated)
export { TypedRPCClient, createTypedRPCClient } from './rpc/typed-rpc-client.js';
export { TypedRPCServer, createTypedRPCServer } from './rpc/typed-rpc-server.js';
```

## 4. 通知/广播消息简化

### 4.1 当前问题

Hub 到 Edge 的通知消息名称不够一致：

```typescript
// 有的用 'hub.' 前缀
'hub.userStateBroadcast'
'hub.channelStateBroadcast'
'hub.userJoined'

// 有的用 'edge.' 前缀
'edge.peerJoined'
'edge.forceDisconnect'
```

### 4.2 建议规范化

```typescript
// 命名规范：
// Hub -> Edge: 'hub.{action}'
// Edge -> Hub: 'edge.{action}'

// Hub 发送的通知（Hub -> Edge）
const HubNotifications = {
  userJoined: 'hub.userJoined',
  userLeft: 'hub.userLeft',
  userStateUpdate: 'hub.userStateUpdate',
  userRemove: 'hub.userRemove',
  channelStateUpdate: 'hub.channelStateUpdate',
  channelRemove: 'hub.channelRemove',
  peerJoined: 'hub.peerJoined',      // 改：原 edge.peerJoined
  peerLeft: 'hub.peerLeft',          // 改：原 edge.peerLeft
  forceDisconnect: 'hub.forceDisconnect', // 改：原 edge.forceDisconnect
  textMessage: 'hub.textMessage',
  aclUpdated: 'hub.aclUpdated',
  voiceRoutingConfig: 'hub.voiceRoutingConfig',
  routeTableUpdate: 'hub.routeTableUpdate',
} as const;
```

## 5. 配置类型统一

### 5.1 当前问题

配置定义分散在多处：

- `edge-server/src/types.ts` - EdgeConfig
- `hub-server/src/types.ts` - HubConfig
- `common/src/types/` - 基础类型
- `protocol/src/` - 共享类型

### 5.2 建议改进

**创建统一的配置 Schema**

```typescript
// common/src/config/schema.ts
import { z } from 'zod'; // 使用 zod 进行运行时验证

// 基础配置
export const NetworkConfigSchema = z.object({
  host: z.string().default('0.0.0.0'),
  port: z.number().min(1).max(65535),
  externalHost: z.string().optional(),
});

export const TLSConfigSchema = z.object({
  enabled: z.boolean().default(true),
  certFile: z.string(),
  keyFile: z.string(),
  caFile: z.string().optional(),
});

// Edge 配置
export const EdgeConfigSchema = z.object({
  server_id: z.number(),
  name: z.string(),
  network: NetworkConfigSchema,
  tls: TLSConfigSchema,
  hubServer: z.object({
    host: z.string(),
    controlPort: z.number(),
    hmacSecret: z.string().optional(),
  }),
  // ...
});

// Hub 配置
export const HubConfigSchema = z.object({
  host: z.string(),
  controlPort: z.number(),
  database: z.object({
    path: z.string(),
    backupDir: z.string(),
    backupInterval: z.number(),
  }),
  // ...
});

// 类型提取
export type EdgeConfig = z.infer<typeof EdgeConfigSchema>;
export type HubConfig = z.infer<typeof HubConfigSchema>;

// 验证函数
export function validateEdgeConfig(config: unknown): EdgeConfig {
  return EdgeConfigSchema.parse(config);
}
```

## 6. 测试辅助简化

### 6.1 测试工厂

当前测试中创建测试对象较繁琐：

```typescript
// 建议：创建测试工厂
// tests/helpers/factories.ts
export const TestFactory = {
  clientInfo(overrides?: Partial<ClientInfo>): ClientInfo {
    return {
      session: 1,
      user_id: 1,
      username: 'testuser',
      channel_id: 0,
      // ...默认值
      ...overrides,
    };
  },
  
  channelInfo(overrides?: Partial<ChannelInfo>): ChannelInfo {
    return {
      id: 1,
      name: 'Test Channel',
      parent_id: 0,
      // ...
      ...overrides,
    };
  },
  
  // 更多工厂方法
};

// 使用
const client = TestFactory.clientInfo({ session: 42 });
```

**注意**：本项目使用集成测试，不使用 mock。测试工厂用于快速创建真实的测试数据对象。

## 7. 代码重复消除

### 7.1 权限检查模式

在多处存在类似的权限检查代码：

```typescript
// 当前：各处重复（control-service.ts 中约有 10+ 处类似代码）
if (this._permissionChecker) {
  const actorUserInfo = HubPermissionChecker.sessionToUserInfo(actorSession, actorSession.channel_id);
  const hasPermission = await this._permissionChecker.hasPermission(
    channelId,
    actorUserInfo,
    Permission.Write
  );
  if (!hasPermission) {
    // 处理拒绝
  }
}
```

**建议：提取通用方法**

```typescript
// hub-server/src/helpers/permission-helper.ts
export class PermissionHelper {
  constructor(
    private permissionChecker: HubPermissionChecker,
    private sessionManager: GlobalSessionManager
  ) {}
  
  async checkPermission(
    actorSessionId: number,
    channelId: number,
    permission: Permission
  ): Promise<{ allowed: boolean; actorSession?: GlobalSession; error?: string }> {
    const actorSession = this.sessionManager.getSession(actorSessionId);
    if (!actorSession) {
      return { allowed: false, error: 'Session not found' };
    }
    
    const actorUserInfo = HubPermissionChecker.sessionToUserInfo(actorSession, actorSession.channel_id);
    const allowed = await this.permissionChecker.hasPermission(channelId, actorUserInfo, permission);
    
    return { allowed, actorSession };
  }
}

// 使用
const { allowed, actorSession, error } = await this.permHelper.checkPermission(
  actorSessionId, channelId, Permission.Write
);
if (!allowed) {
  return { success: false, error };
}
```

**性能影响分析**：

| 方面 | 影响 | 说明 |
|------|------|------|
| 函数调用开销 | 可忽略 | 额外 1 次函数调用，约 0.001ms |
| 对象创建 | 可忽略 | 返回值对象很小（3 个字段） |
| 权限计算 | 无变化 | 底层调用完全相同 |
| 总体影响 | **< 0.01ms** | 相比权限计算本身（可能涉及数据库查询）可忽略 |

**结论**：性能影响可忽略不计。权限检查的主要耗时在于 ACL 遍历和可能的数据库查询，Helper 层的封装开销相比之下微乎其微。

### 7.2 Protobuf 消息构建

重复的 Protobuf 消息构建代码：

```typescript
// 当前：各处重复
const userState = new mumbleproto.UserState({
  session: client.session,
  user_id: client.user_id,
  name: client.username,
  channel_id: client.channel_id,
  // ...
});
```

**建议：创建构建器**

```typescript
// protocol/src/builders/user-state-builder.ts
export class UserStateBuilder {
  static fromClientInfo(client: ClientInfo): mumbleproto.UserState {
    return new mumbleproto.UserState({
      session: client.session,
      user_id: client.user_id,
      name: client.username,
      channel_id: client.channel_id,
      mute: client.mute || undefined,
      deaf: client.deaf || undefined,
      suppress: client.suppress || undefined,
      self_mute: client.self_mute || undefined,
      self_deaf: client.self_deaf || undefined,
      // 只设置有值的字段
    });
  }
  
  static fromGlobalSession(session: GlobalSession): mumbleproto.UserState {
    // ...
  }
}

// 使用
const userState = UserStateBuilder.fromClientInfo(client);
```
